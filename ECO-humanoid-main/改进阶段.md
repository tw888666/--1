# ECO-humanoid BRUCE 能耗建模改进阶段

nullptr，当前项目主线是：把原 ECO（原仓库节能策略）中基于关节侧机械功率绝对值的能耗约束，升级为 BRUCE（机器人名称）电机转子轴端的能耗约束，并分析不同步态阶段下各关节电机的驱动、制动和反向机械功状态。

原始 ECO 能耗约束近似为：

$$
J_{\mathrm{old}} = \sum_j |\tau_j \dot q_j|
$$

当前第一阶段先做到电机转子轴端，不进入电池端模型。当前主指标使用 8 个驱动电机的转子端正向机械能：

$$
E_{\mathrm{rotor,pos,8}} = \sum_t \sum_{m=1}^{8} \max(P^{\mathrm{rotor}}_{m,t}, 0)\Delta t
$$

其中髋偏航两个关节暂时不进入 8 电机约束，但仍记录其正负机械能，方便后续分析。

## 当前状态

当前已经完成到：**阶段 5：`cost_limit1 = 21.45` 短训练试验进行中，已完成 `model_3200.pt` 和 `model_3400.pt` 两轮评估**。

也就是说，现在已经不在“代码能不能跑”的阶段，而是进入了“根据短训练结果判断约束是否真正降低目标能耗”的阶段。

已经完成的关键内容：

1. 已新增电机转子轴端能耗模型。
2. 已保留 `legacy_joint_abs_10` 旧模式，便于和原 ECO 做对照。
3. 已新增 `rotor_positive_8`、`rotor_abs_8`、`joint_positive_8`、`joint_abs_8` 等能耗模式。
4. 已新增 `evaluate_energy.py`，可以输出 `metadata.json`、`episode_summary.csv`、`step_timeseries.csv`、`phase_summary.csv`。
5. 已修复 DOF（自由度）顺序、URDF（统一机器人描述格式）顺序检查、固定指令、随机化关闭、相位统计、终止步状态记录等问题。
6. 已在 A100（服务器图形处理器）或 V100（服务器图形处理器）上完成原始 `model_3000.pt` 的 20 回合能耗评估。
7. 已完成 0.1 m/s 固定速度下的基线数据分析。
8. 已完成 `cost_limit1 = 21.45` 的第一组短训练，训练到 `model_3400.pt` 后总能耗接近基线，单位距离能耗下降，但总正向转子能耗尚未严格低于基线。

当前最重要的基线结果来自：

`eval_ee48bb4_auto_v100_model3000_vx010_20ep`

该结果使用：

- `checkpoint = 3000`
- `command_x = 0.1`
- `command_y = 0`
- `command_yaw = 0`
- `energy_cost_mode = rotor_positive_8`
- `num_eval_episodes = 20`

评估结果：

- 20 回合全部成功。
- 没有摔倒回合。
- `step_timeseries.csv` 中逐步 `cost1` 累加值和 `episode_summary.csv` 中的 `rotor_positive_energy_8` 基本一致。
- 0.1 m/s 数据比 0.15 m/s 数据更适合作为后续训练基准，因为 0.1 m/s 下实际前进速度更接近原训练设定。

基线中位数为：

$$
E_{\mathrm{ref}} = \operatorname{median}(E_{\mathrm{rotor,pos,8}}) = 22.5831
$$

因此，后续 `cost_limit1` 应该围绕这个值设置，而不是沿用旧 ECO 的 `6000`。

当前最新短训练结果来自：

`eval_rp8_cl2145_3400_vx010_20ep`

该结果使用：

- `load_run = rp8_cl2145_200it_s0`
- `checkpoint = 3400`
- `command_x = 0.1`
- `command_y = 0`
- `command_yaw = 0`
- `energy_cost_mode = rotor_positive_8`
- `num_eval_episodes = 20`

评估结论：

- 20 回合全部成功。
- 没有摔倒回合。
- 总正向转子能耗中位数为 `22.6275`，比基线 `22.5831` 高约 `0.20%`，尚未严格通过“总能耗低于基线”的标准。
- 平均速度中位数为 `0.1230 m/s`，比基线 `0.1168 m/s` 更高，说明策略有更明显的速度上飘。
- 单位距离正向转子能耗从 `8.0562` 降到 `7.6635`，下降约 `4.87%`，说明步态效率有改善。
- 左支撑阶段正向转子能耗占比从基线 `53.04%` 增至 `55.42%`，左右支撑能耗不对称仍然存在。

## 阶段 0：代码准备

状态：**已完成**。

目标：保证新能耗模型能正确计算、记录、导出。

已完成内容：

1. 新增转子轴端能耗模型。
2. 支持 10 关节正负机械能统计。
3. 支持 8 驱动电机正负机械能统计。
4. 支持髋偏航关节能量单独记录。
5. 支持多种 `energy_cost_mode`。
6. 支持逐回合、逐步、逐相位统计。
7. 单元测试和语法检查已通过。

## 阶段 1：A100 单回合冒烟测试

状态：**已完成**。

目标：确认 Isaac Gym（机器人仿真平台）真实运行时不会报错，并且能生成完整评估文件。

检查项：

1. 是否能加载 `model_3000.pt`。
2. 是否能固定速度指令。
3. 是否能生成 4 个输出文件。
4. `episode_summary.csv` 是否有回合数据。
5. 能耗是否为正。
6. 机器人是否正常走完或正确记录摔倒。

## 阶段 2：原始策略 20 回合基线评估

状态：**已完成**。

目标：使用原始 `model_3000.pt`，在新能耗定义下记录基线表现。

已经完成两组数据：

1. `eval_ee48bb4_gpu3_model3000_20ep`：0.15 m/s 指令。
2. `eval_ee48bb4_auto_v100_model3000_vx010_20ep`：0.1 m/s 指令。

结论：后续建议以 0.1 m/s 数据作为主要基线，因为原配置中的速度指令就是 `lin_vel_x = [0.1, 0.1]`。

## 阶段 3：原始策略数据分析

状态：**已完成第一轮分析**。

0.1 m/s 基线的关键结果：

| 指标                       |  中位数 |    均值 |
| -------------------------- | ------: | ------: |
| `rotor_positive_energy_8`  | 22.5831 | 22.5768 |
| `rotor_negative_energy_8`  | 23.3949 | 23.3982 |
| `joint_positive_energy_10` | 29.1798 | 29.1776 |
| `joint_negative_energy_10` | 30.4846 | 30.4865 |
| `yaw_positive_energy_2`    |  0.5468 |  0.5455 |
| `distance_x`               |  2.8032 |  2.8035 |
| `mean_velocity_x`          |  0.1168 |  0.1168 |

0.1 m/s 基线下的相位能耗分布：

| 相位                   | 时间占比 | 正向转子能耗占比 |
| ---------------------- | -------: | ---------------: |
| `left_support`         |   40.05% |           53.04% |
| `right_support`        |   40.56% |           31.80% |
| `double_support`       |   19.10% |           14.65% |
| `flight_or_transition` |    0.29% |            0.51% |

当前观察：

1. 左支撑阶段消耗了超过一半的正向转子机械能。
2. 左右支撑阶段存在明显能耗不对称。
3. 反向机械能确实存在，后续可以作为“电机制动/潜在发电状态”的分析对象。
4. 当前分析仍是电机转子轴端机械能，不是电池端能耗。

## 阶段 4：确定 `cost_limit1`

状态：**已完成第一轮候选值确定**。

以 0.1 m/s 原始策略基线为参考：

$$
E_{\mathrm{ref}} = 22.5831
$$

建议候选阈值：

| 目标                   | `cost_limit1` |
| ---------------------- | ------------: |
| 基线附近，基本不压能耗 |         22.58 |
| 轻度节能，约降低 5%    |         21.45 |
| 中等节能，约降低 10%   |         20.32 |
| 较强节能，约降低 15%   |         19.20 |
| 激进节能，约降低 20%   |         18.07 |

也可以写成：

$$
L \in \{22.58, 21.45, 20.32, 19.20, 18.07\}
$$

第一轮不要全部正式长训。建议先做短训练：

1. `cost_limit1 = 22.58`
2. `cost_limit1 = 21.45`
3. `cost_limit1 = 20.32`

如果这三组都能稳定行走，再继续尝试 `19.20` 和 `18.07`。

### `cost_limit1` 与原 ECO 的区别

原 ECO 的 `cost_limit1 = 6000` 和当前短训练使用的 `cost_limit1 = 21.45` 不是同一物理量，也不是同一数值尺度，不能直接做大小比较。

原 ECO 使用的是 10 个关节侧机械功率绝对值：

$$
c_{\mathrm{old},t} = \sum_{j=1}^{10} |\tau_{j,t}\dot q_{j,t}|
$$

也就是把每个关节的正向驱动功率和反向机械功率都取绝对值后相加。这个定义有三个特点：

1. 它在关节侧计算，不在电机转子轴端计算。
2. 它覆盖 10 个关节，包括两个髋偏航关节。
3. 它使用绝对值，因此不能区分正向驱动和反向机械功，也不能单独分析制动或潜在回收能量。

当前主方法使用的是 8 个驱动电机的转子端正向机械能增量：

$$
c_{\mathrm{new},t} = \sum_{m=1}^{8} \max(P^{\mathrm{rotor}}_{m,t}, 0)\Delta t
$$

对应的回合指标是：

$$
E_{\mathrm{rotor,pos,8}} = \sum_t c_{\mathrm{new},t}
$$

它的特点是：

1. 它在电机转子轴端计算，更接近 BRUCE 耦合传动后的电机工作状态。
2. 它只约束 8 个驱动电机，两个髋偏航关节不进入主约束，但仍单独记录正负机械能。
3. 它只累计正向机械能，反向机械功不再被当作“消耗”直接加进主约束。
4. 它包含 $\Delta t$ 积分，因此当前 `cost_limit1` 的量级接近每回合转子端正向机械能，单位可按焦耳解释。

因此：

| 项目 | 原 ECO | 当前阶段 |
| ---- | ------ | -------- |
| 典型 `cost_limit1` | `6000` | `21.45`、`20.32`、`22.58` |
| 成本定义 | 10 关节侧绝对机械功率 | 8 电机转子端正向机械能 |
| 是否区分正负机械功 | 否 | 是 |
| 是否包含髋偏航主约束 | 是 | 否，髋偏航只记录不约束 |
| 是否适合分析制动/反向机械功 | 不适合 | 适合 |
| 数值是否可直接比较 | 不可比较 | 只和同一新定义下的基线比较 |

所以，`cost_limit1 = 21.45` 不是把原 ECO 的 `6000` 简单缩放得到的，而是根据 `rotor_positive_8` 下原始策略 0.1 m/s 评估基线重新校准出来的。当前基线中位数为 `22.5831`，`21.45` 表示希望相对基线约降低 5% 的转子端正向机械能。

## 阶段 5：短训练试验

状态：**进行中，`cost_limit1 = 21.45` 已完成到 `model_3400.pt` 评估**。

短训练的含义：从已有 `model_3000.pt` 继续微调一小段，例如 100、200 或 500 个训练迭代，用来判断约束是否合理。

短训练不是最终论文实验，它的作用是排雷：

1. 检查机器人是否还能稳定走。
2. 检查 `Train/mean_cost1` 是否下降。
3. 检查 `Train/Lagrange1` 是否正常变大。
4. 检查速度跟踪是否明显变差。
5. 检查策略是否通过摔倒、原地踏步或减速来“假节能”。

### 推荐短训练顺序

先跑 `21.45`：**已执行第一组**。

再跑 `20.32`：**暂缓**。当前 `21.45` 尚未严格降低总正向转子能耗，先不要继续加压到 `20.32`。

最后跑 `22.58` 作为“几乎不压能耗”的对照：**暂缓**。需要先判断 `21.45` 在更长短训后是否真正有效。

### 短训练后立刻评估

如果短训练从 `model_3000.pt` 继续 200 轮，通常最终模型会保存为 `model_3200.pt`。短训练结束后，用完全相同条件评估：

`20.32` 和 `22.58` 两组也用同样方式评估

### 已完成短训练记录

#### `cost_limit1 = 21.45`，`model_3200.pt`

训练设置：

- 从 `model_3000.pt` 继续训练。
- 使用 `energy_cost_mode = rotor_positive_8`。
- 使用 `--reset_lagrange_on_resume`，避免继承旧能耗尺度下的拉格朗日乘子。
- 训练约 200 轮后评估 `model_3200.pt`。

评估目录：

`eval_rp8_cl2145_200it_s0_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 |
| -------------------------- | ------: | ------: |
| `rotor_positive_energy_8`  | 23.6883 | 23.6972 |
| `rotor_negative_energy_8`  | 24.4222 | 24.4197 |
| `joint_positive_energy_10` | 31.2083 | 31.2182 |
| `joint_negative_energy_10` | 32.4863 | 32.4834 |
| `distance_x`               |  2.8230 |  2.8235 |
| `mean_velocity_x`          |  0.1176 |  0.1176 |

判断：

1. 20 回合全部成功，没有摔倒。
2. 正向转子总能耗高于基线，短训 200 轮没有通过节能标准。
3. `Train/Lagrange1` 从 `0` 增至约 `0.1687`，约束开始反应，但压力还不够。

#### `cost_limit1 = 21.45`，`model_3400.pt`

训练设置：

- 从 `model_3200.pt` 继续训练。
- 不再使用 `--reset_lagrange_on_resume`，保留已经增长的 `Train/Lagrange1`。
- 继续约 200 轮后评估 `model_3400.pt`。

评估目录：

`eval_rp8_cl2145_3400_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 | 相对基线中位数 |
| -------------------------- | ------: | ------: | -------------: |
| `rotor_positive_energy_8`  | 22.6275 | 22.6144 |        +0.20% |
| `rotor_negative_energy_8`  | 23.4920 | 23.4984 |        +0.41% |
| `joint_positive_energy_10` | 30.0014 | 29.9873 |        +2.82% |
| `joint_negative_energy_10` | 31.2393 | 31.2483 |        +2.48% |
| `yaw_positive_energy_2`    |  0.7090 |  0.7083 |       +29.67% |
| `distance_x`               |  2.9526 |  2.9529 |        +5.33% |
| `mean_velocity_x`          |  0.1230 |  0.1230 |        +5.33% |

补充指标：

| 指标                                  | 基线   | `model_3400.pt` | 变化   |
| ------------------------------------- | -----: | --------------: | -----: |
| $E_{\mathrm{rotor,pos,8}}/d_x$        | 8.0562 |          7.6635 | -4.87% |
| 左支撑正向转子能耗占比               | 53.04% |          55.42% | +2.38% |
| 右支撑正向转子能耗占比               | 31.80% |          28.52% | -3.28% |

判断：

1. 20 回合全部成功，没有摔倒。
2. 相比 `model_3200.pt`，正向转子总能耗明显回落，说明继续训练和拉格朗日乘子增长是有效的。
3. 相比基线，总正向转子能耗仍略高，尚未严格通过“低于 22.5831”的标准。
4. 单位距离正向转子能耗明显降低，但平均速度也从 `0.1168 m/s` 增至 `0.1230 m/s`，需要警惕通过速度上飘掩盖总能耗问题。
5. 左右支撑能耗不对称没有改善，左支撑阶段占比进一步升高。

### 短训练通过标准

短训练是否值得进入正式训练，主要看下面几项：

| 检查项       | 通过标准                                    |
| ------------ | ------------------------------------------- |
| 成功率       | 20 回合中最好全部成功，至少不能明显低于基线 |
| 平均速度     | `mean_velocity_x` 不应明显低于 0.1 m/s      |
| 前进距离     | `distance_x` 不应明显下降                   |
| 正向转子能耗 | `rotor_positive_energy_8` 应低于 22.5831    |
| 拉格朗日乘子 | `Train/Lagrange1` 应对超约束有反应          |
| 行为质量     | 不能靠摔倒、拖步、站着不走来降低能耗        |

如果 `21.45` 能稳定降低能耗且保持速度，说明新约束有效。

如果 `20.32` 明显走坏，说明 10% 节能压力过强，需要放松阈值或延长微调。

如果 `22.58` 和基线几乎一样，这是正常的，因为它基本只是验证新训练管线不会破坏原策略。

当前 `21.45` 的判断是：

1. 稳定性已经通过。
2. `model_3200.pt` 未通过节能标准。
3. `model_3400.pt` 明显改善，但总正向转子能耗仍略高于基线。
4. 在 `model_3400.pt` 之后，建议继续到 `model_3600.pt`，并保持拉格朗日乘子连续增长，不再重置。
5. 如果 `model_3600.pt` 仍不能让总正向转子能耗低于基线，需要考虑提高 `lambda_lr1`，或为短训练建立和评估条件更一致的平地、固定指令训练配置。

## 阶段 6：正式训练

状态：**尚未开始，等待 `21.45` 短训练严格通过后再做**。

正式训练建议从短训练结果里选 1 到 2 个最合适的 `cost_limit1`，再做长训练。

推荐路线：

1. 如果 `21.45` 稳定，先用它做正式训练。
2. 如果 `20.32` 也稳定，再把它作为更强节能版本。
3. 每个关键设置最好至少 3 个随机种子。
4. 如果算力不够，先每组 1 个种子跑通，再补最重要结果的多种子实验。

正式实验至少保留这些对照：

| 组别                  | 说明                               |
| --------------------- | ---------------------------------- |
| Baseline              | 原始 `model_3000.pt`，只评估不训练 |
| `rotor_positive_8`    | 当前主方法                         |
| `joint_positive_8`    | 关节侧正机械能对照                 |
| `rotor_abs_8`         | 转子侧绝对机械能对照               |
| `legacy_joint_abs_10` | 原 ECO 旧定义对照                  |

## 阶段 7：训练后统一评估

状态：**正式训练完成后执行**。

所有模型必须用同一个 `evaluate_energy.py` 和同一组评估条件：

```text
command_x = 0.1
command_y = 0
command_yaw = 0
terrain = plane
noise = off
randomization = off
num_eval_episodes = 20
```

统一比较指标：

1. 成功率。
2. 平均速度。
3. 速度跟踪误差。
4. 回合转子端正向机械能。
5. 回合转子端反向机械能。
6. 10 关节正负机械能。
7. 髋偏航正负机械能。
8. 步态相位能量分布。
9. CoT（运输成本）。

CoT 暂时可以作为辅助指标，但论文中要明确它基于仿真中的质量和机械能估算，不是电池实测能耗。

## 阶段 8：论文结果组织

状态：**训练和评估数据齐全后执行**。

论文可以按下面逻辑组织：

1. 原 ECO 使用关节侧绝对机械功率，不能区分正向驱动和反向机械功。
2. BRUCE 存在耦合传动，因此关节侧功率不能直接表示每个电机的工作状态。
3. 本文先建立理想传动下的电机转子轴端机械功率模型。
4. 原始策略评估证明反向机械功确实存在，并且具有步态相位特征。
5. 新约束训练后，比较能耗、稳定性、速度跟踪和步态相位变化。
6. 明确限制：当前结果不是电池端能耗模型，不含电机效率、驱动器损耗、电池回收效率和热损耗。

## 下一步清单

现在最该做的仍是阶段 5，但已经不是从零开始短训，而是继续判断 `21.45` 是否真正有效：

1. 在远端服务器上从 `model_3400.pt` 继续训练到 `model_3600.pt`。
2. 继续使用 `cost_limit1 = 21.45` 和 `energy_cost_mode = rotor_positive_8`。
3. 不要再使用 `--reset_lagrange_on_resume`，让 `Train/Lagrange1` 延续增长。
4. 训练后用 0.1 m/s、20 回合统一评估 `model_3600.pt`。
5. 把 `eval_rp8_cl2145_3600_vx010_20ep` 传回 Win11（Windows 11 操作系统）。
6. 对比 `episode_summary.csv` 和 `phase_summary.csv`。
7. 如果 `model_3600.pt` 的总正向转子能耗低于 `22.5831`，再考虑跑 `20.32` 或进入更长训练。
8. 如果 `model_3600.pt` 仍只改善单位距离能耗、不降低总能耗，则先不要跑 `20.32`，应检查速度上飘和训练/评估分布不一致问题。

当前推荐判断：

- `21.45` 已经证明不会破坏稳定性，但还没有严格证明能降低总正向转子能耗。
- `20.32` 暂时不要跑，避免在 `21.45` 尚未通过时过早加压。
- `22.58` 可以作为训练管线对照，但优先级低于继续验证 `21.45`。
