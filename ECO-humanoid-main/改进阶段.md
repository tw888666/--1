# ECO-humanoid BRUCE 能耗建模改进阶段

nullptr，当前项目主线是：把原 ECO（原仓库节能策略）中基于关节侧机械功率绝对值的能耗约束，升级为 BRUCE（机器人名称）电机转子轴端的能耗约束，并分析不同步态阶段下各关节电机的驱动、制动和反向机械功状态。

原始 ECO 能耗约束近似为：

$$
J_{\mathrm{old}} = \sum_j |\tau_j \dot q_j|
$$

当前第一阶段先做到电机转子轴端，不进入电池端模型。当前主指标使用 8 个驱动电机的转子端正向机械能：

$$
E_{\mathrm{rotor,pos,8}} = \sum_t \sum_{m=1}^{8} \max(P^{\mathrm{rotor}}_{m,t}, 0)\Delta t
$$

其中髋偏航两个关节暂时不进入 8 电机约束，但仍记录其正负机械能，方便后续分析。

## 当前状态

当前已经推进到：**阶段 6：训练分布校准阈值 `30.884792` 的 `rotor_positive_8` 主方法已经扩展到种子 0 至 5；六种子固定 $v_x=0.1$ 评估中单位距离能耗 6/6 低于基线，训练分布全部回合平均成本 5/6 低于上限，但 seed 1 和 seed 4 仍暴露训练分布稳定性问题**。

也就是说，现在已经从单一种子短训练筛选进入多随机种子稳定性判断和对照组准备阶段。当前结论不是“最终稳定通过”，而是“`rotor_positive_8` 新约束方向有效，固定工况节能比较稳定；训练分布行为质量仍需同时报告成功率和成功回合成本”。

已经完成的关键内容：

1. 已新增电机转子轴端能耗模型。
2. 已保留 `legacy_joint_abs_10` 旧模式，便于和原 ECO 做对照。
3. 已新增 `rotor_positive_8`、`rotor_abs_8`、`joint_positive_8`、`joint_abs_8` 等能耗模式。
4. 已新增 `evaluate_energy.py`，可以输出 `metadata.json`、`episode_summary.csv`、`step_timeseries.csv`、`phase_summary.csv`。
5. 已修复 DOF（自由度）顺序、URDF（统一机器人描述格式）顺序检查、固定指令、随机化关闭、相位统计、终止步状态记录等问题。
6. 已在 A100（服务器图形处理器）或 V100（服务器图形处理器）上完成原始 `model_3000.pt` 的 20 回合能耗评估。
7. 已完成 0.1 m/s 固定速度下的基线数据分析。
8. 已完成 `cost_limit1 = 21.45` 的第一组短训练并推进到 `model_4000.pt`，固定 $v_x=0.1$ 下总正向转子能耗和单位距离能耗均显著低于基线。
9. 已确认 `model_3800.pt` 在固定 $v_x=0.15$ 指令下不能正确跟踪速度，因此当前结论只适用于训练目标 $v_x=0.1$。
10. 已使用 3 个随机种子、共 300 个固定环境完整回合校准训练分布基线，得到平均成本 `32.510307`、成功率 `89.33%` 和 $5\%$ 暂定阈值 `30.884792`。
11. 已确认 `cost_limit1 = 30.884792` 同时重置优化器和拉格朗日乘子的历史实验虽然满足训练分布成本上限，但固定评估单位距离能耗高于基线，训练分布成功率仅为 `86%`。
12. 已完成 `cost_limit1 = 30.884792`、保留原优化器且只重置拉格朗日乘子的受控种子 0 实验。固定评估单位距离能耗下降 `8.30%`，训练分布成功率为 `92%`，全部回合平均成本为 `30.6771`，综合通过。
13. 已完成同一设置的种子 1 实验。固定评估单位距离能耗仅下降 `0.67%`，总正向转子能耗上升 `4.37%`；训练分布全部回合平均成本虽然低于上限，但成功率降至 `84%`，综合未通过。
14. 已完成同一设置的种子 2 实验。固定评估单位距离能耗下降 `4.09%`，总正向转子能耗下降 `4.91%`；训练分布全部回合平均成本为 `30.2717`，成功率为 `90%`，基本通过。
15. 已补充同一设置的种子 3、4、5。固定 $v_x=0.1$ 外部评估中，种子 3、4、5 均 0 摔倒，单位距离能耗分别下降 `6.07%`、`4.93%`、`7.26%`。
16. 六种子固定评估中，单位距离能耗 6/6 低于基线，总正向转子能耗 5/6 低于基线；六种子平均单位距离能耗下降约 `5.22%`。
17. 六种子训练分布校准中，全部回合平均成本 5/6 低于 `30.884792`，seed 4 未通过；成功回合成本只有 seed 5 严格低于上限，说明 `all_episodes.mean` 不能单独作为通过标准。
18. 已完成三个对照模式的原始 `model_3000.pt` 三种子基线校准，得到候选阈值：`rotor_abs_8 = 50.856030`、`joint_positive_8 = 30.905718`、`legacy_joint_abs_10 = 5540.519272`。

当前最重要的基线结果来自：

`eval_ee48bb4_auto_v100_model3000_vx010_20ep`

该结果使用：

- `checkpoint = 3000`
- `command_x = 0.1`
- `command_y = 0`
- `command_yaw = 0`
- `energy_cost_mode = rotor_positive_8`
- `num_eval_episodes = 20`

评估结果：

- 20 回合全部成功。
- 没有摔倒回合。
- `step_timeseries.csv` 中逐步 `cost1` 累加值和 `episode_summary.csv` 中的 `rotor_positive_energy_8` 基本一致。
- 0.1 m/s 数据比 0.15 m/s 数据更适合作为后续训练基准，因为 0.1 m/s 下实际前进速度更接近原训练设定。

基线中位数为：

$$
E_{\mathrm{ref}} = \operatorname{median}(E_{\mathrm{rotor,pos,8}}) = 22.5831
$$

该值适合作为固定 $v_x=0.1$ 外部评估的节能参照，不能直接等同于随机化训练分布下 `Train/mean_cost1` 的基线。新约束不应沿用旧 ECO 的 `6000`，但正式训练前仍需在训练分布下重新校准 `cost_limit1`。

当前最新主评估结果来自：

`eval_rp8_cl2145_4000_vx010_20ep`

该结果使用：

- `load_run = rp8_cl2145_200it_s0`
- `checkpoint = 4000`
- `command_x = 0.1`
- `command_y = 0`
- `command_yaw = 0`
- `energy_cost_mode = rotor_positive_8`
- `num_eval_episodes = 20`

评估结论：

- 20 回合全部成功。
- 没有摔倒回合。
- 总正向转子能耗中位数为 `18.8666`，比基线 `22.5831` 下降约 `16.46%`。
- 平均速度中位数为 `0.1111 m/s`，比基线 `0.1168 m/s` 下降约 `4.83%`，但仍高于目标 `0.1 m/s`，并且比此前模型更接近速度指令。
- 单位距离正向转子能耗从 `8.0562` 降到 `7.0719`，下降约 `12.22%`。
- 训练侧 `Train/Lagrange1` 从迭代 3800 的 `0.502344` 增至迭代 4000 的 `0.561067`，说明约束压力仍在增强。
- 训练侧 `Train/mean_cost1` 从 `26.669615` 降至 `26.395384`，仅下降约 `1.03%`，且仍高于 `cost_limit1 = 21.45`。因此固定 $v_x=0.1$ 外部评估已经通过，训练分布下的形式约束尚未收敛。

## 阶段 0：代码准备

状态：**已完成**。

目标：保证新能耗模型能正确计算、记录、导出。

已完成内容：

1. 新增转子轴端能耗模型。
2. 支持 10 关节正负机械能统计。
3. 支持 8 驱动电机正负机械能统计。
4. 支持髋偏航关节能量单独记录。
5. 支持多种 `energy_cost_mode`。
6. 支持逐回合、逐步、逐相位统计。
7. 单元测试和语法检查已通过。

## 阶段 1：A100 单回合冒烟测试

状态：**已完成**。

目标：确认 Isaac Gym（机器人仿真平台）真实运行时不会报错，并且能生成完整评估文件。

检查项：

1. 是否能加载 `model_3000.pt`。
2. 是否能固定速度指令。
3. 是否能生成 4 个输出文件。
4. `episode_summary.csv` 是否有回合数据。
5. 能耗是否为正。
6. 机器人是否正常走完或正确记录摔倒。

## 阶段 2：原始策略 20 回合基线评估

状态：**已完成**。

目标：使用原始 `model_3000.pt`，在新能耗定义下记录基线表现。

已经完成两组数据：

1. `eval_ee48bb4_gpu3_model3000_20ep`：0.15 m/s 指令。
2. `eval_ee48bb4_auto_v100_model3000_vx010_20ep`：0.1 m/s 指令。

结论：后续建议以 0.1 m/s 数据作为主要基线，因为原配置中的速度指令就是 `lin_vel_x = [0.1, 0.1]`。

## 阶段 3：原始策略数据分析

状态：**已完成第一轮分析**。

0.1 m/s 基线的关键结果：

| 指标                       |  中位数 |    均值 |
| -------------------------- | ------: | ------: |
| `rotor_positive_energy_8`  | 22.5831 | 22.5768 |
| `rotor_negative_energy_8`  | 23.3949 | 23.3982 |
| `joint_positive_energy_10` | 29.1798 | 29.1776 |
| `joint_negative_energy_10` | 30.4846 | 30.4865 |
| `yaw_positive_energy_2`    |  0.5468 |  0.5455 |
| `distance_x`               |  2.8032 |  2.8035 |
| `mean_velocity_x`          |  0.1168 |  0.1168 |

0.1 m/s 基线下的相位能耗分布：

| 相位                   | 时间占比 | 正向转子能耗占比 |
| ---------------------- | -------: | ---------------: |
| `left_support`         |   40.05% |           53.04% |
| `right_support`        |   40.56% |           31.80% |
| `double_support`       |   19.10% |           14.65% |
| `flight_or_transition` |    0.29% |            0.51% |

当前观察：

1. 左支撑阶段消耗了超过一半的正向转子机械能。
2. 左右支撑阶段存在明显能耗不对称。
3. 反向机械能确实存在，后续可以作为“电机制动/潜在发电状态”的分析对象。
4. 当前分析仍是电机转子轴端机械能，不是电池端能耗。

## 阶段 4：确定 `cost_limit1`

状态：**训练分布口径校准已完成，早期固定评估口径候选值仅保留为实验历史**。

早期曾直接以 0.1 m/s 固定评估中的回合正向转子机械能为参考：

$$
E_{\mathrm{ref}} = 22.5831
$$

当时得到的候选数值为：

| 目标                   | `cost_limit1` |
| ---------------------- | ------------: |
| 基线附近，基本不压能耗 |         22.58 |
| 轻度节能，约降低 5%    |         21.45 |
| 中等节能，约降低 10%   |         20.32 |
| 较强节能，约降低 15%   |         19.20 |
| 激进节能，约降低 20%   |         18.07 |

也可以写成：

$$
L \in \{22.58, 21.45, 20.32, 19.20, 18.07\}
$$

这组数值把固定评估能量直接当成了训练成本尺度。后续训练分布校准表明，两者不能直接等同；因此下列顺序只表示当时的短训练计划，不再作为正式训练建议：

1. `cost_limit1 = 22.58`
2. `cost_limit1 = 21.45`
3. `cost_limit1 = 20.32`

实际只执行了 `21.45`，而训练分布三随机种子基线最终给出的 $5\%$ 候选上限是 `30.884792`。因此 `21.45` 应重新解释为强约束，当前不继续尝试 `20.32`、`19.20` 或 `18.07`。

### `cost_limit1` 与原 ECO 的区别

原 ECO 的 `cost_limit1 = 6000` 和当前短训练使用的 `cost_limit1 = 21.45` 不是同一物理量，也不是同一数值尺度，不能直接做大小比较。

原 ECO 使用的是 10 个关节侧机械功率绝对值：

$$
c_{\mathrm{old},t} = \sum_{j=1}^{10} |\tau_{j,t}\dot q_{j,t}|
$$

也就是把每个关节的正向驱动功率和反向机械功率都取绝对值后相加。这个定义有三个特点：

1. 它在关节侧计算，不在电机转子轴端计算。
2. 它覆盖 10 个关节，包括两个髋偏航关节。
3. 它使用绝对值，因此不能区分正向驱动和反向机械功，也不能单独分析制动或潜在回收能量。

当前主方法使用的是 8 个驱动电机的转子端正向机械能增量：

$$
c_{\mathrm{new},t} = \sum_{m=1}^{8} \max(P^{\mathrm{rotor}}_{m,t}, 0)\Delta t
$$

对应的回合指标是：

$$
E_{\mathrm{rotor,pos,8}} = \sum_t c_{\mathrm{new},t}
$$

它的特点是：

1. 它在电机转子轴端计算，更接近 BRUCE 耦合传动后的电机工作状态。
2. 它只约束 8 个驱动电机，两个髋偏航关节不进入主约束，但仍单独记录正负机械能。
3. 它只累计正向机械能，反向机械功不再被当作“消耗”直接加进主约束。
4. 它包含 $\Delta t$ 积分，因此当前 `cost_limit1` 的量级接近每回合转子端正向机械能，单位可按焦耳解释。

因此：

| 项目 | 原 ECO | 当前阶段 |
| ---- | ------ | -------- |
| 典型 `cost_limit1` | `6000` | 强约束 `21.45`、训练分布校准阈值 `30.884792` |
| 成本定义 | 10 关节侧绝对机械功率 | 8 电机转子端正向机械能 |
| 是否区分正负机械功 | 否 | 是 |
| 是否包含髋偏航主约束 | 是 | 否，髋偏航只记录不约束 |
| 是否适合分析制动/反向机械功 | 不适合 | 适合 |
| 数值是否可直接比较 | 不可比较 | 只和同一新定义下的基线比较 |

所以，`cost_limit1 = 21.45` 不是把原 ECO 的 `6000` 简单缩放得到的，而是根据 `rotor_positive_8` 下原始策略 0.1 m/s 固定评估基线计算出来的。当前固定评估基线中位数为 `22.5831`，`21.45` 表示希望外部评估相对基线约降低 5% 的转子端正向机械能。

需要特别注意：算法更新拉格朗日乘子时，直接比较的是随机化训练环境中最近 100 个完成回合的 `Train/mean_cost1`，不是固定评估的中位数。因此，`21.45` 目前同时承担“固定评估目标”和“训练成本上限”两个角色，但两者来自不同数据分布。阶段一可以用它验证约束方向是否有效；正式训练前必须统一或重新校准这两个统计口径。

### 训练分布成本校准工具

已新增 `bruce_gym.scripts.calibrate_train_cost`，用于加载已有策略权重，在不更新策略的情况下按训练分布收集完整回合成本。校准配置只固定 $v_x=0.1$，其余训练条件保持不变，包括：

1. 三角网格训练地形。
2. 观测噪声。
3. 推扰和外部扰动。
4. 质量、摩擦、恢复系数、电机强度、关节摩擦和电枢随机化。
5. 比例与微分增益随机化及控制延迟。
6. 随机航向指令。

脚本默认并行使用 1024 个环境，并在仿真开始前从全部环境中均匀选定 100 个固定环境。每个入选环境只记录第一个完整回合，必须等整个固定样本组结束后再统计；不能直接采用“最先结束的 100 个回合”，否则早摔倒环境会因更快结束而被过度采样。成本采用与 `Train/mean_cost1` 相同的逐步 `cost1` 回合累加方式。

脚本分别报告全部回合和仅成功回合的均值、中位数、总体标准差、最小值和最大值，避免摔倒回合通过缩短时长把成本虚假拉低。如果固定样本组中没有成功回合，`recommended_cost_limit1_all_episodes` 将返回 `null`，并将 `recommendation_status` 标记为 `invalid_no_successful_episodes`；此时不得使用全部摔倒回合计算出的暂定数值。

基于全部完成回合均值的暂定训练成本上限为：

$$
L_{0.95}^{\mathrm{train}} = 0.95\,\overline{C}_{\mathrm{train,3000}}
$$

其中 $\overline{C}_{\mathrm{train,3000}}$ 是原始 `model_3000.pt` 在训练分布下的完整回合平均正向转子机械能。该数值由 `calibration_summary.json` 中的 `recommended_cost_limit1_all_episodes` 给出；在补充多个校准随机种子前，它只作为暂定值。

服务器校准命令：

```bash
CUDA_VISIBLE_DEVICES=0 python -u -m bruce_gym.scripts.calibrate_train_cost \
  --task=bruce_ppolag \
  --headless \
  --resume \
  --experiment_name=exp \
  --load_run=May27_16-06-41_test_a100 \
  --checkpoint=3000 \
  --energy_cost_mode=rotor_positive_8 \
  --calibration_episodes=100 \
  --calibration_limit_fraction=0.95 \
  --num_envs=1024 \
  --command_x=0.1 \
  --sim_device=cuda:0 \
  --rl_device=cuda:0 \
  --seed=0 \
  --output_dir=energy_calibrations/calib_model3000_train_dist_vx010_cohort_s0
```

输出文件包括：

- `episode_costs.csv`：逐回合成本、结果、时长和体坐标系前进速度。
- `calibration_summary.json`：训练分布统计量和暂定 `cost_limit1`。
- `metadata.json`：实际使用的地形、噪声、随机化、模型和指令配置。

#### 三随机种子训练分布基线

对原始 `model_3000.pt` 使用种子 0、1、2，各收集 100 个固定环境的首个完整回合：

| 随机种子 | 全部回合平均成本 | 成功率 |
| -------: | ---------------: | -----: |
| 0        |          33.0677 | 90.00% |
| 1        |          31.9241 | 89.00% |
| 2        |          32.5391 | 89.00% |
| 合并     |          32.5103 | 89.33% |

因此，训练分布下相对基线降低 $5\%$ 的暂定成本上限为：

$$
L_{0.95}^{\mathrm{train}} = 0.95 \times 32.510307 = 30.884792
$$

原来的 `21.45` 相对训练分布合并基线低约 `34.02%`，不是训练分布下的轻度 $5\%$ 目标，因此应归类为强约束实验。

固定环境队列校准同时证明 `calibrate_train_cost` 与训练日志口径一致：强约束 `model_4000.pt` 的训练分布平均成本为 `26.5305`，而同期 `Train/mean_cost1` 约为 `26.4`，两者差异很小。

## 阶段 5：短训练试验

状态：**强约束 `21.45` 已完成种子 0；校准阈值 `30.884792` 的 `rotor_positive_8` 保留优化器受控实验已完成种子 0 至 5 的六种子扩展验证**。

短训练的含义：从已有 `model_3000.pt` 继续微调一小段，例如 100、200 或 500 个训练迭代，用来判断约束是否合理。

短训练不是最终论文实验，它的作用是排雷：

1. 检查机器人是否还能稳定走。
2. 检查 `Train/mean_cost1` 是否下降。
3. 检查 `Train/Lagrange1` 是否正常变大。
4. 检查速度跟踪是否明显变差。
5. 检查策略是否通过摔倒、原地踏步或减速来“假节能”。

### 短训练和评估的完整理解

短训练就是拿原来的策略权重模型，例如 `model_3000.pt`，作为起点，在新的能耗约束条件下继续训练一小段，而不是从零开始训练。

这里新的约束条件主要是：

```text
energy_cost_mode = rotor_positive_8
cost_limit1 = 21.45
```

也就是把原 ECO（原仓库节能策略）里的关节侧绝对机械功率约束，换成 BRUCE（机器人名称）8 个驱动电机转子端正向机械能约束。

短训练的基本流程是：

```text
原始模型 model_3000.pt
  -> 在新 cost_limit1 下继续训练约 200 轮
  -> 得到 model_3200.pt
  -> 继续得到 model_3400.pt、model_3600.pt、model_3800.pt、model_4000.pt
```

短训练不是最终正式实验，它的目的不是直接产出论文结果，而是快速判断：

1. 新约束会不会把机器人训坏。
2. `Train/Lagrange1` 会不会对超约束能耗有反应。
3. 训练中的 `Train/mean_cost1` 或 `rew_cost1` 有没有下降趋势。
4. 策略是不是靠摔倒、减速、原地踏步来假装省能耗。
5. 当前 `cost_limit1` 是否值得进入正式长训练。

评估就是拿短训练后保存出来的权重模型，例如 `model_3200.pt`、`model_3400.pt`、`model_3600.pt`、`model_3800.pt` 或 `model_4000.pt`，放到统一评估环境里跑 20 回合，统计它相对原始 `model_3000.pt` 基线有没有变好。

评估环境固定为：

```text
command_x = 0.1
command_y = 0
command_yaw = 0
terrain = plane
noise = off
randomization = off
num_eval_episodes = 20
```

这样做是为了公平比较。训练时环境更复杂，包含地形、噪声、扰动、延迟、参数随机化和 heading command（航向指令）；评估时必须关掉这些随机因素，否则不同 checkpoint（检查点）之间不好比较。

评估主要看：

1. 成功率和是否摔倒。
2. `mean_velocity_x` 是否保持在合理范围。
3. `distance_x` 是否明显下降。
4. `rotor_positive_energy_8` 是否低于原始基线。
5. `rotor_negative_energy_8` 和 10 关节正负机械能如何变化。
6. `phase_summary.csv` 中步态相位能耗分布是否改善。

最核心的通过标准是：`rotor_positive_energy_8` 低于原始 `model_3000.pt` 的基线 `22.5831`，同时速度和稳定性不能明显变差。

一句话总结：

```text
短训练负责“让策略在新约束下适应一下”；
评估负责“公平检查适应后的策略到底有没有变好”。
```

### 推荐短训练顺序

先跑 `21.45`：**已完成到 `model_4000.pt`，固定 $v_x=0.1$ 外部评估通过并出现单位距离能效平台期**。

再跑 `20.32`：**暂缓**。当前 `21.45` 已明显降低固定 $v_x=0.1$ 的评估能耗，但训练分布下 `Train/mean_cost1` 仍高于 `21.45`，不应立即继续加压。

最后跑 `22.58` 作为“几乎不压能耗”的对照：**暂缓**。当前优先任务是统一训练成本上限和固定工况评估的统计口径。

### 短训练后立刻评估

如果短训练从 `model_3000.pt` 继续 200 轮，通常最终模型会保存为 `model_3200.pt`。短训练结束后，用完全相同条件评估：

`20.32` 和 `22.58` 两组也用同样方式评估

### 已完成短训练记录

#### `cost_limit1 = 21.45`，`model_3200.pt`

训练设置：

- 从 `model_3000.pt` 继续训练。
- 使用 `energy_cost_mode = rotor_positive_8`。
- 使用 `--reset_lagrange_on_resume`，避免继承旧能耗尺度下的拉格朗日乘子。
- 训练约 200 轮后评估 `model_3200.pt`。

评估目录：

`eval_rp8_cl2145_200it_s0_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 |
| -------------------------- | ------: | ------: |
| `rotor_positive_energy_8`  | 23.6883 | 23.6972 |
| `rotor_negative_energy_8`  | 24.4222 | 24.4197 |
| `joint_positive_energy_10` | 31.2083 | 31.2182 |
| `joint_negative_energy_10` | 32.4863 | 32.4834 |
| `distance_x`               |  2.8230 |  2.8235 |
| `mean_velocity_x`          |  0.1176 |  0.1176 |

判断：

1. 20 回合全部成功，没有摔倒。
2. 正向转子总能耗高于基线，短训 200 轮没有通过节能标准。
3. `Train/Lagrange1` 从 `0` 增至约 `0.1687`，约束开始反应，但压力还不够。

#### `cost_limit1 = 21.45`，`model_3400.pt`

训练设置：

- 从 `model_3200.pt` 继续训练。
- 不再使用 `--reset_lagrange_on_resume`，保留已经增长的 `Train/Lagrange1`。
- 继续约 200 轮后评估 `model_3400.pt`。

评估目录：

`eval_rp8_cl2145_3400_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 | 相对基线中位数 |
| -------------------------- | ------: | ------: | -------------: |
| `rotor_positive_energy_8`  | 22.6275 | 22.6144 |        +0.20% |
| `rotor_negative_energy_8`  | 23.4920 | 23.4984 |        +0.41% |
| `joint_positive_energy_10` | 30.0014 | 29.9873 |        +2.82% |
| `joint_negative_energy_10` | 31.2393 | 31.2483 |        +2.48% |
| `yaw_positive_energy_2`    |  0.7090 |  0.7083 |       +29.67% |
| `distance_x`               |  2.9526 |  2.9529 |        +5.33% |
| `mean_velocity_x`          |  0.1230 |  0.1230 |        +5.33% |

补充指标：

| 指标                                  | 基线   | `model_3400.pt` | 变化   |
| ------------------------------------- | -----: | --------------: | -----: |
| $E_{\mathrm{rotor,pos,8}}/d_x$        | 8.0562 |          7.6635 | -4.87% |
| 左支撑正向转子能耗占比               | 53.04% |          55.42% | +2.38% |
| 右支撑正向转子能耗占比               | 31.80% |          28.52% | -3.28% |

判断：

1. 20 回合全部成功，没有摔倒。
2. 相比 `model_3200.pt`，正向转子总能耗明显回落，说明继续训练和拉格朗日乘子增长是有效的。
3. 相比基线，总正向转子能耗仍略高，尚未严格通过“低于 22.5831”的标准。
4. 单位距离正向转子能耗明显降低，但平均速度也从 `0.1168 m/s` 增至 `0.1230 m/s`，需要警惕通过速度上飘掩盖总能耗问题。
5. 左右支撑能耗不对称没有改善，左支撑阶段占比进一步升高。

#### `cost_limit1 = 21.45`，`model_3600.pt`

训练设置：

- 从 `model_3400.pt` 继续训练约 200 轮。
- 保留已有拉格朗日乘子，不再使用 `--reset_lagrange_on_resume`。
- 使用与基线相同的固定 $v_x=0.1$、20 回合评估配置。

评估目录：

`eval_rp8_cl2145_3600_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 | 相对基线中位数 |
| -------------------------- | ------: | ------: | ---------------: |
| `rotor_positive_energy_8`  | 20.3529 | 20.3447 |          -9.88% |
| `rotor_negative_energy_8`  | 21.5535 | 21.5514 |          -7.87% |
| `joint_positive_energy_10` | 27.2087 | 27.2062 |          -6.76% |
| `joint_negative_energy_10` | 28.8204 | 28.8215 |          -5.46% |
| `distance_x`               |  2.8529 |  2.8537 |          +1.77% |
| `mean_velocity_x`          |  0.1188 |  0.1189 |          +1.77% |

补充指标：

| 指标                           | 基线   | `model_3600.pt` | 变化    |
| ------------------------------ | -----: | --------------: | ------: |
| $E_{\mathrm{rotor,pos,8}}/d_x$ | 8.0562 |          7.1340 | -11.45% |

判断：

1. 20 回合全部成功，没有摔倒。
2. 总正向转子能耗首次明显低于基线，且实际速度略高于基线，不是通过减速或缩短距离得到的假节能。
3. 训练侧成本和评估侧能耗同向下降，说明策略已经对新约束产生真实响应。

#### `cost_limit1 = 21.45`，`model_3800.pt`

训练设置：

- 从 `model_3600.pt` 继续训练约 200 轮。
- 继续保留拉格朗日乘子和优化器状态。
- 使用固定 $v_x=0.1$、20 回合评估配置。

评估目录：

`eval_rp8_cl2145_3800_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 | 相对基线中位数 |
| -------------------------- | ------: | ------: | ---------------: |
| `rotor_positive_energy_8`  | 19.6073 | 19.6200 |         -13.18% |
| `rotor_negative_energy_8`  | 20.8097 | 20.8162 |         -11.05% |
| `joint_positive_energy_10` | 26.5277 | 26.5205 |          -9.09% |
| `joint_negative_energy_10` | 28.1365 | 28.1424 |          -7.70% |
| `distance_x`               |  2.7711 |  2.7718 |          -1.15% |
| `mean_velocity_x`          |  0.1154 |  0.1154 |          -1.15% |

补充指标：

| 指标                           | 基线   | `model_3800.pt` | 变化    |
| ------------------------------ | -----: | --------------: | ------: |
| $E_{\mathrm{rotor,pos,8}}/d_x$ | 8.0562 |          7.0757 | -12.17% |

训练侧证据：

| 指标                            | 迭代 3600 | 迭代 3800 | 变化    |
| ------------------------------- | --------: | --------: | ------: |
| `Train/Lagrange1`               |  0.417536 |  0.502344 | +20.31% |
| `Train/mean_cost1`              | 28.635511 | 26.669615 |  -6.87% |
| `Episode/rew_cost1`             |  1.210724 |  1.149678 |  -5.04% |
| `Episode/rew_tracking_lin_vel`  |  0.988060 |  1.004210 |  +1.63% |

判断：

1. 20 回合全部成功，没有摔倒。
2. 相比基线，总正向转子能耗下降约 `13.18%`，单位距离能耗下降约 `12.17%`，平均速度只下降约 `1.15%`。
3. 相比 `model_3600.pt`，总正向转子能耗继续下降约 `3.66%`，但平均速度下降约 `2.87%`，说明继续训练的节能收益已经趋缓并开始带来速度代价。
4. 固定 $v_x=0.1$ 外部评估的正向转子能耗均值为 `19.6200`，数值低于 `21.45`。
5. `cost_limit1` 在算法中直接比较的对象不是上述外部评估中位数或均值，而是最近 100 个训练完成回合的 `Train/mean_cost1`。迭代 3800 时该值为 `26.669615`，仍高于 `21.45`，所以拉格朗日乘子继续上升是正常现象。
6. 准确结论是：固定 $v_x=0.1$ 的短训练节能效果通过，但训练随机化分布下的形式约束尚未收敛。

#### `cost_limit1 = 21.45`，`model_4000.pt`

训练设置：

- 从 `model_3800.pt` 继续训练约 200 轮。
- 继续保留拉格朗日乘子和优化器状态，不使用任何 `--reset_*` 参数。
- 使用固定 $v_x=0.1$、20 回合评估配置。

评估目录：

`eval_rp8_cl2145_4000_vx010_20ep`

评估结果：

| 指标                       |  中位数 |    均值 | 相对基线中位数 |
| -------------------------- | ------: | ------: | ---------------: |
| `rotor_positive_energy_8`  | 18.8666 | 18.8564 |         -16.46% |
| `rotor_negative_energy_8`  | 20.1178 | 20.1120 |         -14.01% |
| `joint_positive_energy_10` | 25.5332 | 25.5264 |         -12.50% |
| `joint_negative_energy_10` | 27.1919 | 27.1946 |         -10.80% |
| `distance_x`               |  2.6678 |  2.6667 |          -4.83% |
| `mean_velocity_x`          |  0.1111 |  0.1111 |          -4.83% |

补充指标：

| 指标                           | 基线   | `model_4000.pt` | 变化    |
| ------------------------------ | -----: | --------------: | ------: |
| $E_{\mathrm{rotor,pos,8}}/d_x$ | 8.0562 |          7.0719 | -12.22% |

训练侧证据：

| 指标                            | 迭代 3800 | 迭代 4000 | 变化    |
| ------------------------------- | --------: | --------: | ------: |
| `Train/Lagrange1`               |  0.502344 |  0.561067 | +11.69% |
| `Train/mean_cost1`              | 26.669615 | 26.395384 |  -1.03% |
| `Episode/rew_cost1`             |  1.149678 |  1.115959 |  -2.93% |
| `Episode/rew_tracking_lin_vel`  |  1.004210 |  0.983325 |  -2.08% |

判断：

1. 20 回合全部成功，没有摔倒。
2. 相比基线，总正向转子能耗下降约 `16.46%`，单位距离能耗下降约 `12.22%`。实际平均速度为 `0.1111 m/s`，仍高于目标 `0.1 m/s`，并且比此前检查点更接近指令。
3. 相比 `model_3800.pt`，总正向转子能耗下降约 `3.78%`，速度和距离下降约 `3.73%`，单位距离能耗仅下降约 `0.05%`。因此新增的总能耗收益主要来自减少速度上飘，而不是进一步提高单位距离效率。
4. `model_4000.pt` 不是通过摔倒、停步或低于目标速度来降低能耗，但单位距离能效已经进入平台期。
5. 恢复训练初期迭代 3820 的成本和速度跟踪奖励同时接近正常值的一半，属于短回合统计瞬态，不用于趋势判断。
6. 迭代 4000 时 `Train/mean_cost1 = 26.395384`，仍比 `21.45` 高约 `23.06%`；拉格朗日乘子继续上升，而训练成本下降已经明显放缓。
7. 当前停止继续到 `model_4200.pt`。`model_4000.pt` 作为固定 $v_x=0.1$ 的最低总能耗候选，`model_3800.pt` 作为单位距离效率几乎相同、速度稍高的备选。

#### `cost_limit1 = 30.884792`，重置优化器的历史种子 0 实验

运行目录：

`logs/exp/rp8_cl30885_400it_s0`

训练设置：

- 从原始 `model_3000.pt` 建立独立运行目录。
- 首次恢复时同时使用 `--reset_optimizer_on_resume` 和 `--reset_lagrange_on_resume`。
- 使用 `energy_cost_mode = rotor_positive_8` 和 `cost_limit1 = 30.884792`。
- 先训练到 `model_3400.pt`，随后在不重置任何状态的情况下继续到 `model_4000.pt`，累计继续训练约 1000 次迭代。
- 训练进程在最终保存后发生 Isaac Gym 原生退出段错误，但 `model_3400.pt` 和 `model_4000.pt` 均可正常加载和评估，因此没有证据表明检查点损坏。

训练趋势：

- 恢复初期迭代 3000 的低成本和低跟踪奖励属于短回合统计瞬态，不用于判断。
- `Train/Lagrange1` 在迭代 3400 前从 `0` 增至 `0.131149`。
- `Train/mean_cost1` 在迭代 3400 前降至 `32.133724`，仍比 `30.884792` 高约 `4.04%`。
- 迭代 4000 的终端日志中 `Episode/rew_cost1 = 1.2917`、`Episode/rew_tracking_lin_vel = 1.0029`，速度跟踪保持稳定。
- `model_4000.pt` 的固定队列训练分布校准成本为 `30.670251`，已经低于 `30.884792`，说明现有算法口径下的形式成本约束已经满足。

固定 $v_x=0.1$、关闭随机化的 20 回合评估：

| 指标                           | 原始 `model_3000.pt` | 校准阈值 `model_3400.pt` | 校准阈值 `model_4000.pt` |
| ------------------------------ | -------------------: | -----------------------: | -----------------------: |
| `rotor_positive_energy_8`      |              22.5831 |                  25.1311 |                  22.0255 |
| `mean_velocity_x`              |               0.1168 |                   0.1175 |                   0.1109 |
| $E_{\mathrm{rotor,pos,8}}/d_x$ |               8.0562 |                   8.9085 |                   8.2697 |
| 摔倒                           |                 0/20 |                     0/20 |                     0/20 |

相对原始基线，校准阈值 `model_4000.pt` 的总正向转子能耗下降约 `2.47%`，但单位距离能耗上升约 `2.65%`。因此它满足训练分布平均成本限制，却没有在固定评估中达到预期的 $5\%$ 单位距离节能目标。

固定队列训练分布校准的种子 0 对比：

| 模型                         | 全部回合平均成本 | 成功回合平均成本 | 成功率 |
| ---------------------------- | ---------------: | ---------------: | -----: |
| 原始 `model_3000.pt`         |          33.0677 |          33.8883 | 90.00% |
| 强约束 `21.45 model_4000.pt` |          26.5305 |          27.7915 | 86.00% |
| 校准阈值 `model_4000.pt`     |          30.6703 |          32.3440 | 86.00% |

校准阈值模型相对同种子基线的全部回合成本下降约 `7.25%`，成功回合成本下降约 `4.56%`，但成功率从 `90%` 降至 `86%`。强约束模型在相同的 `86%` 成功率下具有更低的训练分布成本和固定评估能耗，因此当前种子 0 数据中强约束模型综合占优。

需要保留一个关键实验限制：强约束实验首次恢复时只重置了拉格朗日乘子，而校准阈值实验同时重置了优化器和拉格朗日乘子。两组结果同时受到阈值和优化器恢复方式影响，不能直接解释为纯粹的 `cost_limit1` 因果对照。若要形成严格对照，应从同一个 `model_3000.pt` 重新运行 `30.884792`，保留原优化器状态并只重置拉格朗日乘子。

`model_4000.pt` 校准输出中的 `29.136738` 是在已训练模型成本基础上再降低 $5\%$ 得到的数值，不是原始训练分布基线阈值，不能作为下一轮主实验的 `cost_limit1`。

#### `cost_limit1 = 30.884792`，保留优化器受控实验

为了消除上一组实验中的优化器恢复方式混杂，重新从与强约束实验相同的原始 `model_3000.pt` 开始训练，保留检查点中的优化器状态，只使用 `--reset_lagrange_on_resume`。每个随机种子均继续训练 1000 次迭代并评估 `model_4000.pt`，不继续到 `model_4200.pt`。

运行目录：

- 种子 0：`logs/exp/rp8_cl30885_keepopt_s0`
- 种子 1：`logs/exp/rp8_cl30885_keepopt_s1`
- 种子 2：`logs/exp/rp8_cl30885_keepopt_s2`

已打印的训练趋势显示，拉格朗日乘子能够对超约束成本产生反馈：

- 种子 0 的 `Train/Lagrange1` 在迭代 3040 为 `0`，随后随超约束成本上升，在迭代 3800 达到峰值 `0.274300`；成本降到上限附近后，迭代 4000 回落到 `0.229670`。迭代 4000 的 `Train/mean_cost1 = 30.119940`，低于 `30.884792` 约 `2.48%`，`Episode/rew_tracking_lin_vel = 0.974206`。
- 种子 2 的 `Train/Lagrange1` 从 `0.000000` 上升到迭代 3800 的峰值 `0.283403`，随后在迭代 4000 回落到 `0.222853`。迭代 4000 的 `Train/mean_cost1 = 30.027975`，低于 `30.884792` 约 `2.77%`，`Episode/rew_tracking_lin_vel = 0.985776`。
- 种子 0 和种子 2 都呈现“拉格朗日乘子先升后降、成本降到上限附近、速度跟踪保持稳定”的形态，说明约束反馈是有效的。

固定 $v_x=0.1$、关闭随机化的 20 回合评估：

| 指标                           | 原始 `model_3000.pt` | 保留优化器种子 0 | 保留优化器种子 1 | 保留优化器种子 2 | 三种子平均 |
| ------------------------------ | -------------------: | ----------------: | ----------------: | ----------------: | ----------: |
| `rotor_positive_energy_8`      |              22.5831 |           21.2386 |           23.5697 |           21.4752 |     22.0945 |
| `rotor_negative_energy_8`      |              23.3949 |           22.4902 |           24.5589 |           22.7756 |     23.2749 |
| `joint_positive_energy_10`     |              29.1798 |           28.4794 |           31.3630 |           28.4837 |     29.4420 |
| `distance_x`                   |               2.8032 |            2.8769 |            2.9478 |            2.7795 |      2.8681 |
| `mean_velocity_x`              |               0.1168 |            0.1198 |            0.1228 |            0.1158 |      0.1195 |
| $E_{\mathrm{rotor,pos,8}}/d_x$ |               8.0561 |            7.3877 |            8.0023 |            7.7266 |      7.7055 |
| 摔倒                           |                 0/20 |              0/20 |              0/20 |              0/20 |        0/60 |

种子 0 相对固定评估基线的总正向转子能耗下降 `5.95%`，单位距离能耗下降 `8.30%`，速度提高 `2.63%`。它同时满足节能、速度和零摔倒要求。

种子 1 相对固定评估基线的总正向转子能耗上升 `4.37%`，单位距离能耗仅下降 `0.67%`，速度提高 `5.18%`。它能够稳定行走，但没有复现种子 0 的明显节能效果。

种子 2 相对固定评估基线的总正向转子能耗下降 `4.91%`，单位距离能耗下降 `4.09%`，速度基本持平。它没有种子 0 那么强，但与种子 0 同向支持候选阈值有效。

三种子平均看，固定评估总正向转子能耗比基线下降约 `2.16%`，单位距离能耗下降约 `4.35%`，且 60 个固定评估回合没有摔倒。但由于种子 1 的总能耗高于基线，不能把结果表述为“三随机种子稳定节能”，更准确的表述是“训练分布约束稳定起效，固定工况节能存在随机种子敏感性”。

100 回合训练分布固定队列校准：

| 模型                         | 全部回合平均成本 | 成功回合平均成本 | 成功率 | 摔倒 |
| ---------------------------- | ---------------: | ---------------: | -----: | ---: |
| 原始基线，种子 0             |          33.0677 |          33.8883 | 90.00% |   10 |
| 原始基线，种子 1             |          31.9241 |                 - | 89.00% |   11 |
| 原始基线，种子 2             |          32.5391 |                 - | 89.00% |   11 |
| 原始基线，三种子合并         |          32.5103 |                 - | 89.33% |   32 |
| 保留优化器模型，种子 0       |          30.6771 |          30.9197 | 92.00% |    8 |
| 保留优化器模型，种子 1       |          29.6543 |          31.4500 | 84.00% |   16 |
| 保留优化器模型，种子 2       |          30.2717 |          31.0892 | 90.00% |   10 |
| 保留优化器模型，三种子合并   |          30.2010 |          31.1445 | 88.67% |   34 |

种子 0 的全部回合平均成本低于 `30.884792` 约 `0.67%`，成功率比同种子基线高 2 个百分点，因此综合通过。种子 1 的全部回合平均成本低于上限约 `3.98%`，但成功回合平均成本高于上限，成功率也比同种子基线低 5 个百分点。其较低的全部回合平均成本受到 16 个短摔倒回合影响，因此只能判定为形式成本通过、综合未通过。种子 2 的全部回合平均成本低于上限约 `1.99%`，成功率为 `90%`，比同种子基线高 1 个百分点，因此基本通过。

三种子合并后，保留优化器模型的全部回合平均成本为 `30.2010`，低于上限 `30.884792` 约 `2.21%`，也比原始三种子训练分布基线 `32.5103` 低约 `7.10%`。不过成功回合平均成本为 `31.1445`，略高于 `30.884792`，说明该阈值的形式通过主要对应训练日志的全部回合平均成本口径，而不是“所有成功回合也全部低于上限”的更严格口径。

与重置优化器的历史种子 0 模型相比，保留优化器种子 0 的训练分布全部回合平均成本几乎不变，但成功率从 `86%` 提高到 `92%`，固定评估单位距离能耗下降约 `10.66%`。这说明保留优化器对行为质量和固定工况能效有重要影响。由于两组实验的分段恢复次数仍不同，该结果是强证据，但不能把全部差异唯一归因于优化器状态。

当前第一轮三随机种子验证支持把 `30.884792` 作为候选阈值继续推进，但不支持只用最优种子宣称稳定节能。更稳妥的论文表述应同时报告：训练分布全部回合平均成本 3/3 低于上限；固定 $v_x=0.1$ 工况中种子 0 和种子 2 明显节能，种子 1 暴露随机种子敏感性。

保留优化器模型校准输出中的 `29.143229` 和 `28.171587` 都是在已训练模型成本基础上继续降低 $5\%$ 得到的数值，不是原始训练分布基线阈值，不作为后续训练的 `cost_limit1`。

#### `cost_limit1 = 30.884792`，六随机种子扩展验证

为判断种子 1 是离群还是系统性不稳定，继续补充同一训练协议下的种子 3、4、5。训练协议保持不变：从同一个原始 `model_3000.pt` 恢复，保留优化器，只使用 `--reset_lagrange_on_resume`，训练到 `model_4000.pt`，不继续到 `model_4200.pt`。

新增运行目录：

- 种子 3：`logs/exp/rp8_cl30885_keepopt_s3`
- 种子 4：`logs/exp/rp8_cl30885_keepopt_s4`
- 种子 5：`logs/exp/rp8_cl30885_keepopt_s5`

新增种子 3、4、5 的拉格朗日趋势均显示约束机制本身在工作：

- 种子 3 的 `Train/Lagrange1` 从 `0` 上升到约 `0.266408`，随后回落到 `0.185180`；`Train/mean_cost1` 后期降到 `29.122004`，速度跟踪保持稳定。
- 种子 4 的 `Train/Lagrange1` 从 `0` 上升到约 `0.215722`，随后回落到 `0.163431`；训练日志最后 `Train/mean_cost1 = 30.221794`，但独立 100 回合校准未通过，说明它不是约束无效，而是处在边界附近且泛化到固定队列不稳。
- 种子 5 的 `Train/Lagrange1` 从 `0` 上升到约 `0.231366`，随后回落到 `0.157150`；`Train/mean_cost1` 后期降到 `29.884977`，速度跟踪保持稳定。

固定 $v_x=0.1$、关闭随机化的 20 回合评估扩展为六个训练种子：

| 指标                           | 原始 `model_3000.pt` | 种子 0 | 种子 1 | 种子 2 | 种子 3 | 种子 4 | 种子 5 | 六种子平均 |
| ------------------------------ | -------------------: | -----: | -----: | -----: | -----: | -----: | -----: | ---------: |
| `rotor_positive_energy_8`      |              22.5831 | 21.2386 | 23.5697 | 21.4752 | 21.4375 | 21.6669 | 22.2106 |    21.9331 |
| `distance_x`                   |               2.8032 |  2.8769 |  2.9478 |  2.7795 |  2.8314 |  2.8318 |  2.9718 |     2.8732 |
| `mean_velocity_x`              |               0.1168 |  0.1198 |  0.1228 |  0.1158 |  0.1179 |  0.1179 |  0.1238 |     0.1197 |
| $E_{\mathrm{rotor,pos,8}}/d_x$ |               8.0561 |  7.3877 |  8.0023 |  7.7266 |  7.5674 |  7.6592 |  7.4712 |     7.6357 |
| 摔倒                           |                 0/20 |   0/20 |   0/20 |   0/20 |   0/20 |   0/20 |   0/20 |       0/120 |

固定评估的六种子结论比三种子阶段更强：6/6 种子的单位距离能耗低于基线，5/6 种子的总正向转子能耗低于基线，120 个固定评估回合没有摔倒。六种子平均总正向转子能耗相对基线下降约 `2.88%`，六种子平均单位距离能耗下降约 `5.22%`。

100 回合训练分布固定队列校准扩展如下：

| 模型                       | 全部回合平均成本 | 成功回合平均成本 | 成功率 | 摔倒 | 对 `30.884792` 的判断 |
| -------------------------- | ---------------: | ---------------: | -----: | ---: | --------------------- |
| 保留优化器模型，种子 0     |          30.6771 |          30.9197 | 92.00% |    8 | 全部回合通过，成功回合略高 |
| 保留优化器模型，种子 1     |          29.6543 |          31.4500 | 84.00% |   16 | 全部回合通过，综合未通过 |
| 保留优化器模型，种子 2     |          30.2717 |          31.0892 | 90.00% |   10 | 全部回合通过，成功回合略高 |
| 保留优化器模型，种子 3     |          30.1948 |          31.0422 | 89.00% |   11 | 全部回合通过，成功回合略高 |
| 保留优化器模型，种子 4     |          31.2370 |          32.3977 | 86.00% |   14 | 全部回合和成功回合均未通过 |
| 保留优化器模型，种子 5     |          29.0449 |          30.5030 | 91.00% |    9 | 全部回合和成功回合均通过 |
| 保留优化器模型，六种子合并 |          30.1800 |          31.2203 | 88.67% |   68 | 全部回合通过，成功回合略高 |

因此，六种子结果支持以下更细的判断：

1. 固定 $v_x=0.1$ 外部评估中，新约束方向比较稳定，单位距离能耗 6/6 低于基线。
2. 训练分布全部回合平均成本 5/6 低于 `30.884792`，但 seed 4 明确未通过。
3. 成功回合平均成本只有 seed 5 严格低于 `30.884792`；seed 1 和 seed 4 的成功率也偏低，说明不能只用 `all_episodes.mean` 判断约束是否成功。
4. seed 1 不再是唯一异常，seed 4 也暴露训练分布稳定性问题。当前更准确的结论是：主方法固定工况节能有效，但训练分布行为质量仍有随机种子敏感性。

#### 对照组训练分布基线校准

为了准备正式对照实验，已经对三个对照模式的原始 `model_3000.pt` 做了种子 0、1、2 的训练分布基线校准。不同 `energy_cost_mode` 的成本尺度不同，因此不能共用 `rotor_positive_8` 的 `30.884792`。

| 模式                  | 三种子全部回合基线均值 | 基线成功率 | 候选 `cost_limit1` |
| --------------------- | ---------------------: | ---------: | -----------------: |
| `rotor_positive_8`    |                32.5103 |     89.33% |          30.884792 |
| `rotor_abs_8`         |                53.5327 |     87.00% |          50.856030 |
| `joint_positive_8`    |                32.5323 |     86.00% |          30.905718 |
| `legacy_joint_abs_10` |              5832.1256 |     87.67% |        5540.519272 |

`rotor_abs_8` 的成本尺度明显大于 `rotor_positive_8`，因为它把转子端正向和反向机械功都计入绝对值。`joint_positive_8` 与 `rotor_positive_8` 的数值尺度接近，适合作为“关节侧正机械能”和“转子端正机械能”的直接对照。`legacy_joint_abs_10` 的成功回合平均成本在种子 0、1、2 上约为 `6019` 至 `6064`，与原 ECO 常用的 `cost_limit1 = 6000` 同量级，说明旧 ECO 成本尺度已经基本复现。

当前建议优先训练 `rotor_abs_8` 对照组，因为它与主方法只差“正向机械能”与“绝对机械能”的定义，最能直接验证为什么主方法只约束正向机械功。

#### 固定 $v_x=0.15$ 的边界检查

已有原始 `model_3000.pt` 的 $v_x=0.15$ 评估，因此不需要重复基线测试。`model_3800.pt` 的检查目录为：

`eval_rp8_cl2145_3800_vx015_20ep`

虽然目录名包含 `20ep`，但 `metadata.json` 记录实际只完成 5 回合。结果如下：

| 指标                     | `model_3000.pt` | `model_3800.pt` | 变化    |
| ------------------------ | --------------: | --------------: | ------: |
| 实际平均速度             |          0.0592 |          0.0462 | -21.97% |
| `rotor_positive_energy_8` |         16.9446 |         15.2416 | -10.05% |
| 单位距离正向转子能耗     |         11.9216 |         13.7433 | +15.28% |
| 摔倒                     |            0/20 |             0/5 |       — |

绝对能耗下降主要来自实际行走速度和距离下降，单位距离能耗反而恶化，因此不能解释为 $v_x=0.15$ 节能成功。当前训练配置的前进速度范围固定为 $[0.1,0.1]$，所以 $v_x=0.15$ 属于训练分布之外。阶段 5 的有效结论应明确限定在 $v_x=0.1$。

### 短训练通过标准

短训练是否值得进入正式训练，主要看下面几项：

| 检查项       | 通过标准                                    |
| ------------ | ------------------------------------------- |
| 成功率       | 20 回合中最好全部成功，至少不能明显低于基线 |
| 平均速度     | `mean_velocity_x` 不应明显低于 0.1 m/s      |
| 前进距离     | `distance_x` 不应明显下降                   |
| 正向转子能耗 | `rotor_positive_energy_8` 应低于 22.5831    |
| 拉格朗日乘子 | `Train/Lagrange1` 应对超约束有反应          |
| 行为质量     | 不能靠摔倒、拖步、站着不走来降低能耗        |

如果 `21.45` 能稳定降低能耗且保持速度，说明新约束有效。

如果 `20.32` 明显走坏，说明 10% 节能压力过强，需要放松阈值或延长微调。

如果 `22.58` 和基线几乎一样，这是正常的，因为它基本只是验证新训练管线不会破坏原策略。

当前判断是：

1. 强约束 `21.45` 的 `model_4000.pt` 仍是当前固定 $v_x=0.1$ 总能耗和单位距离能耗最低的候选模型。
2. 强约束模型在训练分布下的成功率为 `86%`，低于同种子原始基线的 `90%`，并且 `Train/mean_cost1` 仍高于 `21.45`；它是有效的强压力实验，但没有满足自身形式成本上限。
3. 校准阈值 `30.884792` 的保留优化器模型在六随机种子固定 $v_x=0.1$ 评估中，单位距离能耗 6/6 低于基线，平均下降约 `5.22%`，说明新约束方向在固定工况下有效。
4. 六种子训练分布全部回合平均成本 5/6 低于上限，seed 4 未通过；成功回合平均成本只有 seed 5 严格低于上限，说明 `all_episodes.mean` 只能对应训练日志口径，不能单独代表行为质量通过。
5. seed 1 和 seed 4 暴露出成功率偏低、成功回合成本偏高的问题。当前不能宣称论文级稳定通过，但可以支撑“新约束方向有效，固定评估节能稳定性较好”的阶段性结论。
6. 保留优化器比重置优化器的种子 0 模型表现更好，说明优化器恢复方式确实是重要实验因素；后续实验必须统一为保留优化器、只重置拉格朗日乘子。
7. 三个对照模式的基线阈值已经完成校准，下一步优先训练 `rotor_abs_8` 对照组，而不是继续把现有主方法 run 拉到 `model_4200.pt`。

## 阶段 6：正式训练

状态：**主方法六随机种子扩展验证已完成；对照组基线阈值已经完成校准，下一步进入 `rotor_abs_8` 对照训练**。

当前 `rotor_positive_8` 主方法固定使用 `cost_limit1 = 30.884792`、保留优化器并只重置拉格朗日乘子的恢复方式。对照组不能共用该阈值，必须使用各自训练分布基线校准得到的 `cost_limit1`。

推荐路线：

1. 保留强约束 `21.45` 的 `model_4000.pt`、校准阈值的历史重置优化器模型，以及保留优化器种子 0 至 5 的 `model_4000.pt`，不继续训练覆盖。
2. 将 `30.884792` 暂定为 `rotor_positive_8` 主方法的候选阈值，而不是最终最优阈值。
3. 论文或组会汇报中同时给出逐种子结果、六种子平均值、成功率和失败种子的解释，不用种子 0 单独代表方法效果。
4. 使用各对照模式自己的基线阈值训练对照组；优先顺序建议为 `rotor_abs_8`、`joint_positive_8`、`legacy_joint_abs_10`。

正式实验至少保留这些对照：

| 组别                  | 说明                               |
| --------------------- | ---------------------------------- |
| Baseline              | 原始 `model_3000.pt`，只评估不训练 |
| `rotor_positive_8`    | 当前主方法                         |
| `joint_positive_8`    | 关节侧正机械能对照                 |
| `rotor_abs_8`         | 转子侧绝对机械能对照               |
| `legacy_joint_abs_10` | 原 ECO 旧定义对照                  |

## 阶段 7：训练后统一评估

状态：**正式训练完成后执行**。

所有模型必须用同一个 `evaluate_energy.py` 和同一组评估条件：

```text
command_x = 0.1
command_y = 0
command_yaw = 0
terrain = plane
noise = off
randomization = off
num_eval_episodes = 20
```

统一比较指标：

1. 成功率。
2. 平均速度。
3. 速度跟踪误差。
4. 回合转子端正向机械能。
5. 回合转子端反向机械能。
6. 10 关节正负机械能。
7. 髋偏航正负机械能。
8. 步态相位能量分布。
9. CoT（运输成本）。

CoT 暂时可以作为辅助指标，但论文中要明确它基于仿真中的质量和机械能估算，不是电池实测能耗。

## 阶段 8：论文结果组织

状态：**训练和评估数据齐全后执行**。

论文可以按下面逻辑组织：

1. 原 ECO 使用关节侧绝对机械功率，不能区分正向驱动和反向机械功。
2. BRUCE 存在耦合传动，因此关节侧功率不能直接表示每个电机的工作状态。
3. 本文先建立理想传动下的电机转子轴端机械功率模型。
4. 原始策略评估证明反向机械功确实存在，并且具有步态相位特征。
5. 新约束训练后，比较能耗、稳定性、速度跟踪和步态相位变化。
6. 明确限制：当前结果不是电池端能耗模型，不含电机效率、驱动器损耗、电池回收效率和热损耗。

## 下一步清单

保留优化器受控实验已经完成种子 0 至 5 的六种子扩展验证，对照组基线阈值也已经完成校准。下一步集中做结果冻结和对照组训练：

1. 在远端服务器保留所有现有 `model_4000.pt` 和强约束 `model_3800.pt`，不要用重复训练覆盖。
2. 将 `cost_limit1 = 30.884792` 记录为 `rotor_positive_8` 主方法的候选阈值。六种子固定评估单位距离能耗 6/6 低于基线，但训练分布全部回合平均成本只有 5/6 低于上限。
3. 对外汇报时同时说明 seed 1 和 seed 4 的弱点：训练分布成功率偏低，成功回合成本偏高，说明行为质量仍有随机种子敏感性。
4. 优先训练 `rotor_abs_8` 对照组，使用它自己的 `cost_limit1 = 50.856030`，不使用主方法的 `30.884792`。
5. 之后依次训练 `joint_positive_8` 和 `legacy_joint_abs_10` 对照组，阈值分别为 `30.905718` 和 `5540.519272`。
6. 当前不采用校准输出中的 `29.143229`、`28.171587`、`28.758124`、`27.592638`、`28.685058` 或 `29.675132` 作为新主阈值，因为它们是在已训练模型成本基础上继续降低 $5\%$ 得到的数值，不是原始训练分布基线阈值。
7. 当前目标只要求 $v_x=0.1$，不把分布外的 $v_x=0.15$ 作为阶段一通过条件。

当前推荐判断：

- 强约束 `21.45` 的 `model_4000.pt` 是当前最低能耗候选，但没有满足自身形式成本上限，训练分布成功率为 `86%`。
- 校准阈值 `30.884792` 是当前最合理的 `rotor_positive_8` 主候选阈值：它来自原始训练分布三随机种子基线，并且六种子固定评估中单位距离能耗全部低于基线。
- 训练分布下 seed 1 和 seed 4 暴露成功率和成功回合成本问题，因此当前应报告为“主方法固定工况节能效果稳定，但训练分布行为质量仍存在随机种子敏感性”。
- 如果只要求阶段一证明新约束有效，当前主方法结果已经足够支撑方向有效；如果要求论文级方法优势，下一步必须补齐 `rotor_abs_8`、`joint_positive_8` 和 `legacy_joint_abs_10` 对照组。
