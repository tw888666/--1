# BRUCE 电机转子轴端能耗改进计划

## 1. 研究目标

将原始关节侧绝对机械功率约束

$$
C_{\mathrm{old}}=\sum_j\left|\tau_j\dot q_j\right|
$$

升级为考虑 BRUCE 并联传动结构的电机转子轴端机械能耗约束。具体目标如下：

1. 将关节转矩和关节速度映射到 8 个 Koala BEAR 电机的转子轴。
2. 训练时仅约束转子正机械能，即电机主动驱动机器人时输出的机械能。
3. 单独记录转子负机械能，用于分析潜在发电或机械制动状态。
4. 分析不同步态阶段中各电机的驱动、制动、保持和空闲状态。
5. 暂不建模电机铜耗、铁耗、驱动器效率、电池回充效率和电池端净能耗。
6. 两个髋偏航舵机暂不纳入主能耗约束，但在评估阶段监测其关节侧功率，防止策略将能耗转移到未约束关节。

论文中应将研究对象表述为“电机转子轴端机械能耗”和“潜在再生机械能”，不能表述为真实电能或电池能耗。

## 2. BRUCE 传动模型

### 2.1 髋部并联传动

每条腿的髋俯仰和髋侧摆关节由两个 Koala BEAR 电机通过差动机构驱动。关节速度和减速器输出轴速度满足：

$$
\dot q_h=J_h\dot\theta_h
$$

其中：

$$
J_h=
\begin{bmatrix}
-0.5 & -0.5 \\
0.5 & -0.5
\end{bmatrix}
$$

变量定义为：

$$
q_h=
\begin{bmatrix}
q_{\mathrm{hip\ pitch}} \\
q_{\mathrm{hip\ roll}}
\end{bmatrix}
$$

$$
\theta_h=
\begin{bmatrix}
\theta_{h,1} \\
\theta_{h,2}
\end{bmatrix}
$$

### 2.2 膝踝并联传动

膝关节和踝关节通过四连杆机构与两个 Koala BEAR 电机耦合，满足：

$$
\dot q_l=J_l\dot\theta_l
$$

其中：

$$
J_l=
\begin{bmatrix}
-1 & 0 \\
1 & 1
\end{bmatrix}
$$

变量定义为：

$$
q_l=
\begin{bmatrix}
q_{\mathrm{knee}} \\
q_{\mathrm{ankle}}
\end{bmatrix}
$$

$$
\theta_l=
\begin{bmatrix}
\theta_{l,1} \\
\theta_{l,2}
\end{bmatrix}
$$

### 2.3 速度和转矩映射

从关节侧映射到减速器输出轴：

$$
\dot\theta=J^{-1}\dot q
$$

根据虚功关系，输出轴转矩为：

$$
\tau_\theta=J^\mathsf{T}\tau_q
$$

Koala BEAR 的减速比为 $N=9$，因此转子轴转速和转矩为：

$$
\omega_m=N\dot\theta
$$

$$
\tau_m=\frac{\tau_\theta}{N}
$$

逐电机转子机械功率定义为：

$$
P_{m,j}=\tau_{m,j}\omega_{m,j}
$$

在理想传动条件下，三个层级必须满足功率守恒：

$$
\tau_q^\mathsf{T}\dot q
=
\tau_\theta^\mathsf{T}\dot\theta
=
\tau_m^\mathsf{T}\omega_m
$$

需要注意：理想减速器不会改变机械功率。转子轴模型与原始关节模型之间的主要差异来自并联传动后的逐电机功率分解，而不是简单地将原功率乘以减速比。

## 3. 新能耗约束

### 3.1 正向驱动机械能

训练约束仅累计 8 个 Koala BEAR 电机的转子正机械能：

$$
E_{\mathrm{drive},k}
=
\sum_s\sum_{j=1}^{8}
\max\left(P_{m,j,s},0\right)\Delta t_{\mathrm{sim}}
$$

其中，$s$ 表示一个控制周期内的物理仿真子步，$\Delta t_{\mathrm{sim}}=0.001\,\mathrm{s}$。

### 3.2 潜在制动机械能

转子负机械功率不计入训练成本，而是单独累计：

$$
E_{\mathrm{brake},k}
=
\sum_s\sum_{j=1}^{8}
\max\left(-P_{m,j,s},0\right)\Delta t_{\mathrm{sim}}
$$

该指标只能说明电机正在吸收机械能。由于缺少电机电流、绕组电阻、驱动器拓扑和回充效率数据，不能将其直接解释为实际回收电能。

### 3.3 时间积分和单位

当前控制周期为 $0.01\,\mathrm{s}$，每个控制周期包含 10 个 $1\,\mathrm{kHz}$ 物理子步。新的 `cost1_buf` 应返回这 10 个物理子步积分后的 $E_{\mathrm{drive},k}$，单位为焦耳。

不应继续只使用最后一个物理子步的瞬时功率，否则会产生采样误差，也无法严格解释约束阈值的物理单位。

## 4. 代码修改

### 4.1 能耗计算模块

新增统一的转子轴端能耗计算模块，输入为：

- 10 个腿部关节的关节转矩；
- 10 个腿部关节的关节速度；
- 髋部和膝踝传动矩阵；
- Koala BEAR 减速比；
- 物理仿真时间步长。

输出为：

- 8 个电机的减速器输出轴转矩和速度；
- 8 个电机的转子转矩和转速；
- 8 个电机的有符号转子机械功率；
- 每个控制周期的正向驱动机械能；
- 每个控制周期的潜在制动机械能。

### 4.2 环境修改

修改环境物理步进过程：

1. 在每个 $1\,\mathrm{kHz}$ 物理子步中计算转子轴端功率。
2. 在一个控制周期开始时清零能量缓存。
3. 在 10 个物理子步中累计正向驱动机械能和潜在制动机械能。
4. 将控制周期结束时的正向驱动机械能赋给 `cost1_buf`。
5. 保留原始约束计算模式，以支持回归测试和消融实验。

### 4.3 日志修改

评估日志应增加：

- 逐电机转子转矩；
- 逐电机转子转速；
- 逐电机有符号机械功率；
- 逐电机正向驱动机械能；
- 逐电机潜在制动机械能；
- 两个髋偏航关节的关节侧有符号功率；
- 左右足接触状态和垂直接触力；
- 当前步态相位和速度命令。

同时修复 `play.py` 中第 10 个关节功率错误使用第 9 个关节转矩的索引问题。

## 5. 消融实验

所有消融方法均使用相同的 8 个 Koala BEAR 执行器：

| 方法 | 约束定义 | 研究作用 |
| --- | --- | --- |
| `JointAbs-8` | 8 个关节坐标的绝对机械功率 | 接近原始 ECO 的基线 |
| `JointPositive-8` | 8 个关节坐标的正机械功率 | 分析去除负功惩罚的作用 |
| `RotorAbs-8` | 8 个电机转子绝对机械功率 | 分析并联传动逐电机分解的作用 |
| `RotorPositive-8` | 8 个电机转子正机械功率 | 本文主方法 |

原始 `3000.pt` 用于：

- 复现原始 ECO 行为；
- 验证新旧指标计算；
- 生成初步步态分析图；
- 检查代码修改是否破坏已有推理流程。

原始 `3000.pt` 不应作为新方法最终统计结论的主要证据。

## 6. 阈值搜索

首先使用无能耗约束或原始策略，计算新指标下的参考能量 $E_{\mathrm{ref}}$。随后搜索：

$$
b_1\in
\left\{
0.8E_{\mathrm{ref}},
0.6E_{\mathrm{ref}},
0.4E_{\mathrm{ref}}
\right\}
$$

选择满足以下条件的最低阈值：

1. 回合完成率不低于 $90\%$。
2. 平均速度跟踪误差不超过目标速度的 $10\%$。
3. 机器人没有通过增加髋偏航功率规避主约束。
4. 训练过程没有出现长期约束违反或拉格朗日乘子发散。

## 7. 训练与评估流程

### 7.1 小规模验证

1. 使用单环境验证张量维度、左右腿顺序和功率守恒。
2. 使用原始 `3000.pt` 比较关节侧和转子轴端指标。
3. 使用 3 个随机种子训练 1000 次迭代。
4. 检查策略是否能够稳定行走，以及正机械能是否下降。
5. 小规模试验通过后再开始完整实验。

### 7.2 正式训练

- 每种方法使用 5 个随机种子。
- 每个随机种子训练 3000 次迭代。
- 保持奖励函数、镜像约束、网络结构和域随机化设置一致。
- 保存训练配置、代码版本、随机种子、训练日志和模型检查点。

四种方法共需要完成 20 次正式训练。

### 7.3 评估条件

分别在以下目标速度下评估：

$$
v_x\in
\left\{
0.1,\,
0.15,\,
0.2
\right\}\,\mathrm{m/s}
$$

每个策略和速度组合至少运行 20 个完整回合，并进行：

- Isaac Gym（英伟达 GPU 并行机器人仿真平台）评估；
- MuJoCo（多关节动力学仿真器）仿真到仿真评估；
- 外部推力扰动评估；
- 不同摩擦系数和质量参数下的鲁棒性评估。

## 8. 平台分工

### 8.1 Windows 11

Windows 11 上完成：

- 论文阅读和数学模型推导；
- Python（编程语言）代码编写；
- NumPy（数值计算库）矩阵验证；
- PyTorch（深度学习框架）CPU 单元测试；
- `3000.pt` 文件结构和网络权重检查；
- MuJoCo 少量单策略测试；
- CSV（逗号分隔值）日志处理；
- 统计分析、绘图和论文写作。

### 8.2 A100 服务器

A100 服务器上完成：

- Isaac Gym 单环境和少量环境验证；
- 4096 或 8192 个并行环境训练；
- 能耗阈值搜索；
- 多随机种子小规模训练；
- 20 次正式训练；
- 批量速度、扰动和域随机化评估；
- 大规模结果导出。

## 9. 步态阶段分析

### 9.1 步态阶段划分

优先根据实际足部垂直接触力划分步态，而不是只依赖预设时钟：

- 双支撑阶段；
- 左单支撑阶段；
- 右单支撑阶段；
- 腾空或接触转换阶段。

以同一只脚连续两次稳定触地作为一个完整步态周期，并将周期归一化为 $0\%-100\%$。

### 9.2 电机工作状态

每个电机按转子功率和转速划分为：

- 驱动：$P_m>\varepsilon_P$；
- 潜在发电或制动：$P_m<-\varepsilon_P$；
- 保持：$\left|\omega_m\right|<\varepsilon_\omega$ 且转矩显著；
- 空闲：其余状态。

初始阈值设置为：

$$
\varepsilon_P=10^{-3}\,\mathrm{W}
$$

$$
\varepsilon_\omega=10^{-2}\,\mathrm{rad/s}
$$

保持状态的转矩阈值设为对应电机允许输出转矩的 $5\%$，并在数据分析时检查阈值敏感性。

### 9.3 输出图表

论文至少应包含：

1. 各电机在归一化步态周期内的有符号功率曲线。
2. 各电机的转矩—转速四象限图。
3. 电机工作状态随步态阶段变化的概率热力图。
4. 各电机正向驱动机械能占比。
5. 各电机潜在制动机械能占比。
6. 不同约束方法下的能量分布对比。
7. 髋偏航关节功率监测结果。

## 10. 主要评估指标

- 回合完成率；
- 平均速度跟踪误差；
- 扰动恢复成功率；
- 转子正向驱动机械能；
- 转子潜在制动机械能；
- 潜在制动能量占总绝对机械能的比例；
- 各电机能耗占比；
- 峰值转子功率；
- 转子功率均方根；
- 髋偏航关节正机械能；
- 运输成本。

运输成本定义为：

$$
\mathrm{COT}
=
\frac{E_{\mathrm{drive}}}{mgd}
$$

其中，$m$ 为机器人质量，$g$ 为重力加速度，$d$ 为机器人行走距离。

正式结果应报告均值、标准差、$95\%$ 置信区间和效应量。

## 11. 单元测试与回归测试

必须验证：

1. 两个传动矩阵的逆矩阵计算正确。
2. 关节速度能够正确映射到输出轴和转子轴。
3. 关节转矩能够按照虚功关系正确映射。
4. 左右腿的关节顺序和符号正确。
5. 减速比换算不会改变理想机械功率。
6. 正功和负功分别积分，不发生相互抵消。
7. 恒定功率输入经过固定时间积分后得到正确能量。
8. 旧约束模式能够复现原始实现的数值定义。

功率守恒的相对误差要求为：

$$
\varepsilon_{\mathrm{power}}
=
\frac{
\left|
P_q-P_m
\right|
}{
\max\left(\left|P_q\right|,10^{-8}\right)
}
<
10^{-6}
$$

## 12. 验收标准

主方法 `RotorPositive-8` 应满足：

1. 相比 `JointAbs-8`，转子正向驱动机械能至少降低 $10\%$。
2. 回合完成率、速度跟踪和扰动恢复性能下降不超过 $5\%$。
3. 两个髋偏航关节的正机械能没有显著异常增加。
4. 多随机种子结果的 $95\%$ 置信区间支持节能改进结论。
5. 至少一个主要电机呈现与步态阶段一致的驱动和制动状态变化。
6. Isaac Gym 和 MuJoCo 中的指标变化趋势一致。

## 13. 论文结构建议

1. 引言：指出关节绝对机械功率不能反映 BRUCE 并联传动中的逐电机工作状态。
2. 相关工作：介绍人形机器人能耗优化、约束强化学习和再生制动分析。
3. BRUCE 转子轴端模型：介绍传动矩阵、减速器和功率守恒。
4. 转子正机械能约束：介绍新成本函数及其物理意义。
5. 实验设计：介绍消融方法、阈值搜索和统计方案。
6. 实验结果：报告稳定性、能耗和跨仿真结果。
7. 步态阶段分析：解释各电机的驱动与制动状态。
8. 局限性：说明尚未建模电损耗、驱动器和电池系统。
9. 结论：总结转子轴端约束相对原始关节侧约束的价值。

## 14. 结论边界

本研究能够证明：

- 并联传动会改变逐电机机械功率分布；
- 转子正机械能约束可以影响策略的驱动能量使用方式；
- 不同步态阶段中的电机驱动和制动状态可以被定量分析。

本研究暂时不能证明：

- 电机实际输入电能降低；
- 负机械功率全部转化为电能；
- 电池获得了对应的回充能量；
- 策略已经实现电池供电侧能耗最优。
