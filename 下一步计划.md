nullptr，按照[共享对话](https://chatgpt.com/s/t_6a2fc1131bf88191bdc574f8775b42b4)的研究逻辑，并结合当前代码，建议严格按下面顺序进行。

**阶段一：完善评估代码（Win11）**

1. 创建实验分支：

```powershell
git checkout -b energy-evaluation-stage1
```

2. 新建 `bruce_gym/scripts/evaluate_energy.py`，不要继续把正式统计塞进 `play.py`。

3. 评估脚本增加 CLI（命令行界面）参数：

```text
--num_eval_episodes
--command_x
--command_y
--command_yaw
--output_dir
--energy_cost_mode
```

4. 固定评估环境：

```python
num_envs = 1
terrain = "plane"
noise = False
push_robots = False
disturbance = False
delay = False
randomize_kp = False
randomize_kd = False
randomize_motor_strength = False
```

5. 第一轮使用训练配置对应的指令：

```text
command_x = 0.1 m/s
command_y = 0
command_yaw = 0
episode_length = 24 s
```

当前 `play.py` 默认的 `0.15 m/s` 与训练配置的 `0.1 m/s` 不一致，不适合作为第一组基准。

**阶段二：补齐需要记录的能量**

在 [rotor_energy.py](C:/Users/95781/Desktop/阶段1/ECO-humanoid-main/bruce_gym/rotor_energy.py:126) 增加全部10个关节的子步能量：

$$
E_{j,+}=\max(\tau_j\dot q_j,0)\Delta t
$$

$$
E_{j,-}=\max(-\tau_j\dot q_j,0)\Delta t
$$

然后在 [legged_robot.py](C:/Users/95781/Desktop/阶段1/ECO-humanoid-main/bruce_gym/envs/base/legged_robot.py:151) 增加并累计：

```text
joint_drive_energy_per_joint：10维
joint_brake_energy_per_joint：10维
rotor_drive_energy_per_motor：8维
rotor_brake_energy_per_motor：8维
yaw_joint_drive_energy：2维
yaw_joint_brake_energy：2维
```

每个策略步开始时清零，每个 $0.001\,\mathrm{s}$ 物理子步累加。

**阶段三：处理回合终止**

当前 `done=True` 时：

- 返回的能量仍属于终止步，可以累计；
- 环境状态已经被自动重置，不能用于步态相位分析。

第一版评估采用：

```python
episode_energy += current_step_energy

if not done:
    保存关节、接触和相位数据
```

CSV（逗号分隔值）中增加：

```text
done
timeout
valid_state
episode_id
episode_step
```

其中 `done=True` 时设置 `valid_state=0`。后续需要精确保留终止状态时，再通过 `extras["terminal_state"]` 在重置前保存状态。

**阶段四：生成评估文件**

建议每次评估生成四个文件：

```text
metadata.json
episode_summary.csv
step_timeseries.csv
phase_summary.csv
```

`episode_summary.csv` 至少包含：

```text
episode_id
timeout
fall
duration_s
distance_x
mean_velocity_x
rotor_positive_energy_8
rotor_negative_energy_8
yaw_positive_energy_2
yaw_negative_energy_2
joint_positive_energy_10
joint_negative_energy_10
每个电机的正负机械能
```

`step_timeseries.csv` 记录功率曲线、接触状态和步态相位。

**阶段五：Win11 本地验证**

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
python -m py_compile bruce_gym\rotor_energy.py bruce_gym\scripts\evaluate_energy.py
git diff --check
```

还要检查：

$$
E_{\mathrm{abs}}\approx E_+ + E_-
$$

$$
E_{\mathrm{signed}}\approx E_+ - E_-
$$

以及理想传动下总有符号功率是否守恒。

**阶段六：在 A100 上运行原始策略**

拉取代码并准备 checkpoint（检查点）：

```bash
git pull
mkdir -p logs/exp/baseline_3000
cp model_3000.pt logs/exp/baseline_3000/model_3000.pt
```

检查环境：

```bash
python -c "import torch,numpy; print(torch.__version__, torch.version.cuda, numpy.__version__); print(torch.cuda.get_device_name(0))"
```

运行20个回合：

```bash
python bruce_gym/scripts/evaluate_energy.py \
  --task=bruce_ppolag \
  --load_run=baseline_3000 \
  --checkpoint=3000 \
  --energy_cost_mode=rotor_positive_8 \
  --num_eval_episodes=20 \
  --command_x=0.1 \
  --headless
```

正常超时的回合算成功回合；提前跌倒的回合单独统计，不能偷偷删除。

**阶段七：确定约束阈值**

只使用成功完成24秒的回合，计算转子正机械能中位数：

$$
E_{\mathrm{ref}}=\operatorname{median}(E_{\mathrm{rotor},8}^{+})
$$

初始候选阈值建议为：

$$
L\in\{1.0E_{\mathrm{ref}},\,0.9E_{\mathrm{ref}},\,0.8E_{\mathrm{ref}}\}
$$

`cost_limit1` 的单位是 $\mathrm{J/episode}$，不要使用旧的 `6000`，也不要使用平均每步能量。

**阶段八：短程训练**

每个阈值先训练300次迭代，不使用 `--resume`：

```bash
python bruce_gym/scripts/train.py \
  --task=bruce_ppolag \
  --headless \
  --num_envs=2048 \
  --max_iterations=300 \
  --energy_cost_mode=rotor_positive_8 \
  --cost_limit1=<计算出的阈值> \
  --run_name=rotor_pos8_Lxxx_seed1 \
  --seed=1 \
  --sim_device=cuda:0 \
  --rl_device=cuda:0
```

使用 TensorBoard（训练可视化工具）检查：

```text
Train/mean_cost1
Train/Lagrange1
Mean reward
Mean episode length
速度跟踪误差
跌倒率
```

若能耗下降但回合长度明显缩短，说明机器人可能通过跌倒规避能耗约束。

**阶段九：正式实验**

短训练通过后再进行：

- 3000次迭代；
- 至少3个随机种子；
- 原始 `legacy_joint_abs_10` 基线；
- `joint_positive_8` 消融；
- `rotor_positive_8` 主实验；
- 所有模型使用相同评估指令和环境。

你现在最先要完成的是：**实现 `evaluate_energy.py`、补齐10关节积分能量，并成功生成20个回合的 `episode_summary.csv`。**
