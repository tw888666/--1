# BRUCE 转子轴端能耗下一步计划

## 1. 当前判断

这份计划的总体方向合理：先把电机转子轴端机械能模型做成可验证、可记录、可切换的代码路径，再用原始策略校准阈值，最后才启动正式训练。

需要调整的地方有三点：

1. 原文引用的远程仓库、提交号和本地路径不是当前工作区，应改为当前目录 `C:\Users\95781\Desktop\阶段1\ECO-humanoid-main`。
2. `cost_limit1` 的含义不是单个物理子步阈值，而是训练日志中按回合累计后的平均 episode cost（回合成本）阈值。
3. 部分已列为待修的问题已经在本次修改中完成，应从阻塞项移到已完成项。

## 2. 当前代码状态

已完成的核心改动：

1. 新增 `bruce_gym/rotor_energy.py`，实现 10 个腿部关节到 8 个 Koala BEAR 电机转子轴端的理想传动映射。
2. `LeggedRobot.step()` 已在每个 $0.001\,\mathrm{s}$ 物理子步中累计能量。
3. `cost1_buf` 在 `rotor_positive_8` 模式下表示一个策略控制周期内的转子正机械能，单位为 $\mathrm{J}$。
4. 已保留 `legacy_joint_abs_10`、`joint_abs_8`、`joint_positive_8`、`rotor_abs_8` 和 `rotor_positive_8` 五种模式。
5. `play.py` 已记录转子转矩、转速、有符号功率、正向能量、反向能量、髋偏航功率、触地状态和步态相位。
6. 已修复 `play.py` 中第 10 个关节功率误用第 9 个关节转矩的问题。
7. 已新增不依赖 Isaac Gym（英伟达 GPU 并行机器人仿真平台）的 PyTorch（深度学习框架）CPU（中央处理器）单元测试。

本次继续完成的修正：

1. `--cost_limit1` 已同时同步到 `env_cfg.env.cost_limit1` 和 `train_cfg.algorithm.cost_limit1`。
2. 新增 `--energy_cost_mode` CLI（命令行界面）参数，便于同一代码路径下切换消融模型。
3. `OnPolicyRunner.load()` 已按实际保存的 `lagrange1`、`lagrange1_lambda_optimizer` 等键恢复拉格朗日对象，并兼容旧键名。
4. `OnPolicyRunner.load()` 使用当前 runner 设备做 `map_location`，不再固定到 `cuda:0`。
5. `play.py` 的 CSV（逗号分隔值）输出已增加 `episode_id`、`episode_step` 和 `done`，便于回合级统计。

## 3. 能耗指标解释

旧约束为 10 个关节侧瞬时绝对机械功率：

$$
c_t^{\mathrm{legacy}}=\sum_{j=1}^{10}\left|\tau_j\dot q_j\right|
$$

主方法约束为 8 个耦合驱动电机的转子正机械能：

$$
c_t^{\mathrm{rotor+}}
=
\sum_{k=1}^{N_{\mathrm{sub}}}
\sum_{m=1}^{8}
\max\left(P_{m,k},0\right)\Delta t
$$

训练中的拉格朗日乘子更新使用回合累计成本：

$$
J_C=\sum_{t=0}^{T-1}c_t
$$

因此，`cost_limit1` 应按完整回合能量来校准，而不是直接沿用旧的 `6000`，也不应凭直觉改成某个单步能量值。

当前模型可称为：

> 理想传动模型下的电机转子轴端正机械能。

当前模型不能称为真实电能或电池能耗，因为尚未包含铜耗、铁耗、驱动器效率、减速器效率、电池充放电效率和转子惯量项。

## 4. 主约束边界

当前主约束明确限定为 8 个耦合驱动电机，即 `rotor_positive_8`。

两个髋偏航轴暂不加入 `cost1_buf`，只在评估中监测其关节侧功率和能量。论文表述应写成“8 个主要耦合驱动电机的转子轴端机械能”，不能写成“全机器人 10 电机总能耗”。

如果后续发现策略明显把能量转移到髋偏航轴，再新增独立模式，例如 `rotor_positive_10`，不要直接改变 `rotor_positive_8` 的定义。

## 5. 剩余阻塞项

### P0：校准 `cost_limit1`

`cost_limit1 = 6000` 仍只是旧配置遗留值。正式训练前必须先用原始 `model_3000.pt` 在 A100 Linux 服务器上统计新指标下的完整回合能量分布。

建议固定速度命令和随机种子，至少采集 20 个成功完整回合，统计：

- 每回合 8 电机正机械能 $E_8^+$；
- 每回合 8 电机反向机械能 $E_8^-$；
- 两个髋偏航轴正机械能；
- 平均正机械功率；
- 行走距离；
- 正机械运输成本。

正机械运输成本定义为：

$$
\mathrm{CoT}^+=\frac{E^+}{mgd}
$$

以基线回合能量中位数 $E_{\mathrm{ref}}$ 为参考，优先测试：

$$
E_{\max}\in
\left\{
1.0E_{\mathrm{ref}},
0.8E_{\mathrm{ref}},
0.6E_{\mathrm{ref}}
\right\}
$$

### P0：确认 Isaac Gym 回放链路

Win11 原生环境不适合作为 Isaac Gym 正式回放环境。A100 上需要先验证：

1. 能加载 `model_3000.pt`。
2. 能跑固定策略回放。
3. `cost1_buf`、转子功率日志和回合累计 CSV 均有正常数值。
4. 同一检查点和同一随机种子下，多次运行的统计量稳定。

### P1：补关节顺序断言

在环境初始化后增加断言，确认 `dof_names` 与能耗模块默认索引一致：

| 索引 | 期望关节 |
|---|---|
| 0 | `hip_yaw_l` |
| 1 | `hip_pitch_l` |
| 2 | `hip_roll_l` |
| 3 | `knee_pitch_l` |
| 4 | `ankle_pitch_l` |
| 5 | `hip_yaw_r` |
| 6 | `hip_pitch_r` |
| 7 | `hip_roll_r` |
| 8 | `knee_pitch_r` |
| 9 | `ankle_pitch_r` |

如果顺序不一致，应立刻报错，而不是静默计算错误电机能耗。

### P1：写独立评估脚本

`play.py` 适合人工观察和临时导出，但正式统计建议新增脚本，例如 `bruce_gym/scripts/evaluate_energy.py`。

该脚本应支持：

1. 固定 `energy_cost_mode`。
2. 固定速度命令。
3. 指定回合数。
4. 只统计成功完整回合。
5. 同时导出 step-level（步级）CSV 和 episode-level（回合级）CSV。
6. 输出均值、标准差、中位数、四分位数和 95% 置信区间。

### P1：补环境级累计测试

已有测试覆盖了纯数学映射，还需要在不依赖 GPU 的范围内继续增加：

1. 控制周期开始时能量缓存清零。
2. 10 个子步能量正确累计。
3. `legacy_joint_abs_10` 保持旧瞬时功率定义。
4. `rotor_positive_8` 输出非负能量。
5. `rotor_abs_8` 等于正向能量与反向能量之和。

### P2：区分训练配置与实验命名

所有正式训练目录名应包含：

- 能耗模式；
- 阈值；
- 随机种子；
- 训练算法；
- 代码提交号或日期。

推荐格式：

```text
rotor_positive_8_b<value>_seed<seed>_ppolag_<date>
```

### P2：处理 PPO 奖励塑形基线

`_reward_energy()` 当前仍是关节侧绝对功率：

$$
r_{\mathrm{energy}}
=
\sum_j\left|\tau_j\dot q_j\right|
$$

这可以保留为原始 PPO（近端策略优化）奖励塑形基线。若要做公平的转子轴端奖励塑形比较，应新增一个单独奖励项，不要覆盖原始定义。

## 6. 推荐执行顺序

### 阶段 A：本地收尾

1. 增加关节顺序断言。
2. 增加环境级累计测试。
3. 编写 `evaluate_energy.py` 的初版接口。
4. 再跑一次单元测试、语法检查和 `git diff --check`。

阶段 A 的验收标准：

1. 不启动 Isaac Gym 也能验证数学映射与缓存逻辑。
2. `--cost_limit1` 和 `--energy_cost_mode` 能正确覆盖配置。
3. 检查点恢复逻辑能恢复全部拉格朗日对象。

### 阶段 B：A100 固定策略评估

1. 按仓库要求确认 NumPy（数值计算库）、PyTorch、CUDA（统一计算设备架构）和 Isaac Gym 版本。
2. 使用 `model_3000.pt` 跑固定策略。
3. 对 $v_x=0.1\,\mathrm{m/s}$、$v_x=0.15\,\mathrm{m/s}$、$v_x=0.2\,\mathrm{m/s}$ 分别采集回合。
4. 生成 `legacy_joint_abs_10`、`joint_positive_8`、`rotor_abs_8` 和 `rotor_positive_8` 的离线指标对比。
5. 确定第一轮 `cost_limit1` 候选值。

### 阶段 C：短程训练冒烟测试

先运行 300 到 500 次迭代，检查：

1. 平均回合能量是否接近约束。
2. 拉格朗日乘子是否合理变化。
3. 速度跟踪是否明显崩溃。
4. 是否通过频繁跌倒或缩短回合降低能量。
5. 保存后重新加载是否能连续恢复训练状态。

### 阶段 D：正式消融实验

第一轮固定约束算法，只比较能耗模型：

| 组别 | 能耗模型 | 目的 |
|---|---|---|
| A | `legacy_joint_abs_10` | 原始 ECO 基线 |
| B | `joint_positive_8` | 分离正负功率影响 |
| C | `rotor_positive_8` | 主方法 |
| D | `rotor_abs_8` | 转子绝对机械功率对照 |

每组至少 3 个随机种子；资源允许时扩展到 5 个随机种子。

### 阶段 E：步态阶段分析

根据左右脚接触状态划分：

- 双支撑；
- 左腿单支撑；
- 右腿单支撑；
- 腾空或接触转换。

电机状态初步定义为：

$$
\mathrm{state}_m=
\begin{cases}
\mathrm{drive}, & P_m>\varepsilon_P \\
\mathrm{reverse}, & P_m<-\varepsilon_P \\
\mathrm{idle}, & |P_m|\le\varepsilon_P
\end{cases}
$$

$\varepsilon_P$ 应根据仿真噪声和功率分布确定，并做阈值敏感性检查。

## 7. 近期不建议做的事

1. 不要直接用 `6000` 启动正式 3000 次迭代训练。
2. 不要把当前指标称为电池能耗。
3. 不要把 `rotor_positive_8` 写成全机器人 10 电机总能耗。
4. 不要在没有回合级统计的情况下比较策略能耗。
5. 不要用未修复或未验证的恢复逻辑继续训练旧检查点并得出正式结论。

## 8. 下一步最小任务

最小闭环任务如下：

1. 在本地补关节顺序断言和环境级累计测试。
2. 在 A100 上用 `model_3000.pt` 跑 20 个完整回合。
3. 生成 episode-level CSV。
4. 用回合能量中位数确定第一组 `cost_limit1`。
5. 做 300 到 500 次迭代短程训练。

完成这五项后，再决定是否进入正式多随机种子训练。
